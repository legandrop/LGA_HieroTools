v3.92:

        - El Flow Pull dispara Fix Colorspaces al terminar, si el proyecto esta color managed (`LGA_NKS_Flow_Pull v3.62`, `LGA_NKS_FixColorspaces v1.22`). El Pull cambia los clips a su version mas alta, y en Hiero una `Version` distinta es OTRO Clip con su propio color transform: cada bump devolvia el clip al espacio del archivo y deshacia la correccion. Se extrajo `run_if_color_managed()`, que ahora comparten el boton del Edit Panel y el Pull -una sola implementacion-, y corre SOLO el camino managed: si el proyecto no esta configurado no hace nada, y en particular no cae al barrido viejo de rec709, que como efecto secundario de un pull seria una sorpresa fea. Solo se dispara si el Pull cambio algo. Import diferido y a prueba de fallos: si la correccion falla, el Pull sigue igual. [ HieroTools - El Flow Pull corrige los colorspaces al terminar ]

        - El disparo post-pull no abre grupo de undo propio (`LGA_NKS_Flow_Pull v3.62`). La primera version abria uno, porque en `LGA_NKS_Flow_Pull.py` no hay ningun `beginUndo` y parecia que el Pull no tenia: lo tiene, pero lo abre el LLAMADOR, en `LGA_NKS_Flow_Panel.py`, donde `run_FPT_pull()` y `run_FPT_pull_with_deselect()` envuelven `FPT_Hiero()` en `beginUndo("Run External Script")`. Anidar dos fusiona los macros y rompe el Ctrl+Z, que es justo lo que el repo evita a proposito. Ahora el undo lo abre siempre el llamador y un Ctrl+Z despues de un Pull deshace la operacion completa, versiones y colorspaces juntos. De paso, `_volcar_log()` pasa a `volcar_log()`: la llama otro modulo y no puede ser privada. [ HieroTools - El fix post-pull deja de anidar el grupo de undo ]

        - Fix Colorspaces pasa a usar el color management del proyecto (`LGA_NKS_FixColorspaces v1.21`, `LGA_NKS_ColorManagement_Config v1.00`): si el proyecto tiene los tres espacios configurados desde el tab Projects de PipeSync y estan habilitados, el boton recorre los tracks de la secuencia activa y aplica el espacio de `plates` a los tracks que terminan en "plate" y el de `exr_publish` a los de `TASK_EXR_TRACKS` -importada de `LGA_NKS_GetClip`, no duplicada-. La config se lee de `project_settings_cache` de `pipesync_stats.db` en modo read-only, con el patron de `LGA_NKS_Vendors_Config`. Como el colorspace vive en el Clip del bin y no en el TrackItem, primero se agrupan todos los clips por regla: uno que cae bajo dos reglas distintas queda en conflicto y no se toca. El token se resuelve contra el config OCIO del clip con el algoritmo de dos pasadas de `LGA_ApplyAMF`, no comparando strings. Si el proyecto no esta managed, el boton hace exactamente lo de antes. Doc nuevo: `docs/Docu_Color_Management.md`. [ HieroTools - Fix Colorspaces usa el color management del proyecto ]

        - Dos huecos de robustez de Fix Colorspaces que salieron en la auditoria (`LGA_NKS_FixColorspaces v1.21`). El camino viejo barria `hiero.core.projects()` entero, o sea TODOS los proyectos abiertos: con un proyecto managed abierto al lado de uno que no lo esta, apretar el boton le pisaba el colorspace de los plates al managed, sin aviso y sin pasar por la deteccion de conflictos. Ahora saltea los clips que pertenecen a un proyecto managed, resolviendo el proyecto por la ruta del media. Y el camino managed atrapa sus propias excepciones e informa por consola: antes cualquier fallo no contemplado se lo tragaba `execute_external_script()` del panel, que devuelve True igual, y el boton no hacia nada sin decir nada. Ante el error NO cae al camino viejo, a proposito: correr el barrido de rec709 sobre un proyecto managed es justo lo que el filtro evita. [ HieroTools - Fix Colorspaces no pisa proyectos managed ni falla mudo ]

        - El tooltip de Fix Colorspaces decia solo "Detecta y corrige clips con colorspace rec709 o gamma2.2", que dejo de describir lo que hace el boton. Ahora nombra los dos caminos. [ HieroTools - Tooltip de Fix Colorspaces al dia ]

        - Create EXR v000 creaba el v000 con el nombre equivocado cuando el vendor code del shot no estaba cargado en la DB de PipeSync (`LGA_NKS_CreateV000 v1.17`): el shot code se parsea del nombre del plate, y sin el vendor en la lista el bloque base se calcula de tres, asi que con un plate real -`PROJA_1013_0800_VEN_aPlate_v001.%04d.exr`- los dos bloques que sobran disparan la regla de shot con descripcion y el sufijo del plate entra al nombre. Salia `PROJA_1013_0800_VEN_aPlate_comp_v000`, sin aviso. Ahora, cuando la carpeta del shot y el parseo son prefijo por bloques uno del otro, manda la carpeta: sale de cortar la ruta en `_input` y no depende de la DB, igual que hacen Import Shot y Create NK. El camino afectado era abrir Create v000 solo; lanzada desde Import Shot ya recibia el nombre de la carpeta como hint. [ HieroTools - Create v000 conserva el vendor code del shot ]

        - El toggle Studio/Client del Projects Panel pasa a una fila propia ARRIBA de la lista de proyectos, alineado a la izquierda (`LGA_NKS_UIManager v1.05`). Antes compartia la fila del contador, debajo de la lista, lejos de lo que gobierna. La fila del toggle solo se crea si el toggle existe, asi que para los logins que no lo ven la lista sigue pegada al borde superior como estaba. [ HieroTools - Toggle de contexto arriba de la lista ]

        - Create NK v000 destaca los denoised faltantes y la conservacion de rutas originales en el cartel de confirmacion, usando el enfasis del pack para reconocer el problema de un vistazo. [ HieroTools - Resaltar denoised en la confirmacion ]

        - Create NK v000 permite continuar cuando faltan denoised (`LGA_NKS_CreateNKScript v1.12`): una sola confirmacion lista todos los faltantes de los plates con slot en el template. Al aceptar conserva los Reads y sus rutas originales, sin reemplazar el shot dentro de esas rutas, y los informa al finalizar. Cancelar no escribe el script. [ HieroTools - Crear NK con denoised pendientes ]

v3.91:

        - Create NK v000 corria el EditRef dos veces (`LGA_NKS_CreateNKScript v1.11`): al TimeClip se le escribia el rango YA colocado en el timeline (1005-2082) y ademas quedaba con `frame_mode "start at"` en ese mismo 1005, asi que el corrimiento se aplicaba dos veces y su `original range` declaraba frames que el Read del mov no entrega (el Read sale 1-1078). Ahora el TimeClip lleva el rango REAL del clip, el mismo del Read, y quien lo ubica en el timeline es unicamente el "start at". El Write de review sigue acotado a los frames de timeline, que es lo que corresponde. [ HieroTools - TimeClip del EditRef con el rango real del clip ]

        - Nuevo `docs/Docu_Look_Files_y_Plates.md`: la convencion de la carpeta de look no estaba documentada en ningun lado, solo en los docstrings de las dos tools que dependen de ella -Apply AMF de HieroTools y su contraparte de ToolPack-B, que ademas la resuelven distinto-. El doc explica que hay un .amf por PLATE y no por shot, que el .cdl es hermano del .amf y el .clf uno solo compartido, por que en Hiero se deduce el plate del clip y en Nuke se pregunta, las trampas medidas (mayusculas mezcladas en la misma carpeta, prerenders con dos _v###, el .cdl ambiguo) y por que hay cache. [ HieroTools - Documentar la convencion de Look_Files y plates ]

        - Apply AMF le ponia a un clip el grade de OTRO plate (`LGA_NKS_ApplyAMF v0.92`): el .amf y el .cdl se resolvian como "el unico archivo de esa extension" y con varios se quedaba con el primero alfabetico, pero un shot trae un .amf POR PLATE y cada plate tiene su propio grade. Ahora se toma el del plate del clip, en su version mas alta; si no hay uno para ese plate, o el clip no es un plate -un _comp-, se usa el del aPlate, y si tampoco hay, el primero. El .cdl pasa a ser el HERMANO del .amf elegido. De paso el toggle se hizo mas rapido: listaba la carpeta y parseaba los XML POR CLIP, y ahora lo hace una vez por corrida y por plate. Sobre veinte clips, de 8,5 ms a 1,6 ms. [ HieroTools - Apply AMF resuelve el .amf por el plate del clip ]

        - Apply AMF procesaba el .clf del LMT en el espacio equivocado (`LGA_NKS_ApplyAMF v0.91`): el nodo quedaba con su default `scene_linear`, que no es un espacio sino un ROL del config OCIO, y en los configs ACES apunta a ACEScg (AP1). El .clf entra y sale en ACES2065-1, asi que recibia el gamut equivocado. La causa: el working space se seteaba solo cuando el .amf lo declaraba, y el unico que lo declara es el CDL, via <cdlWorkingSpace>, que es la EXCEPCION para sacarlo a ACEScct. La cadena corre en ACES2065-1 y ahora ese es el default. El matcheo contra el enum deja afuera los roles, y si el config no expone el espacio pedido eso cuenta como error y sube al cartel, en vez de un WARN del log con el efecto informado como creado. El CDL no cambia. [ HieroTools - Apply AMF procesa el CLF en ACES2065-1 ]

        - El modulo de estilo suma `FRAME_RANGE_GRADIENT`, los dos colores del rango de frames de una secuencia (`LGA_UI_Style_HieroTools v1.27`): el Media Manager de LGA_ToolPack pasa a pintar el `[1001-1129]` con el gradiente violeta-fucsia del browser de FileManager S3, y los colores entran al modulo para que una secuencia se lea igual en Nuke que en la app. Es un cambio de paridad: las cuatro copias del modulo se mueven juntas y en HieroTools todavia no lo consume nadie. Sin cambios de codigo ejecutable. [ HieroTools - FRAME_RANGE_GRADIENT en el modulo de estilo ]

        - Create NK v000 dejaba sin CDL/LUT a los nodos de color de un template nuevo (`LGA_NKS_CreateNKScript v1.10`): en ese template se borraron las expresiones TCL que resolvian el look, y Nuke no escribe el knob `file` cuando queda en su valor default, asi que tres de los cuatro nodos venian directamente SIN ese knob. El criterio para reconocer un nodo del look exigia que el path dijera algo, y esos tres quedaban intactos. Ahora un `file` vacio o ausente tambien cuenta como del look -un CDL con `read_from_file` y sin archivo esta esperando uno- y el knob se agrega si no estaba. Un `file` que apunta a un archivo concreto fuera de Look_Files se sigue respetando. [ HieroTools - Nodos de color sin knob file en Create NK v000 ]

        - Create NK v000 no registraba nada de los nodos de color y un reporte de un usuario no se podia diagnosticar (`LGA_NKS_CreateNKScript v1.09`): decia que los OCIO de adentro del grupo no se le reemplazaban, y el log no tenia una sola linea al respecto -ese registro existio y se perdio en una pasada anterior-. Ahora el log dice, por CADA nodo de color: cual es, si estaba suelto o dentro de que grupo, y si se reemplazo o quedo intacto y por que. Ademas el criterio para reconocer un nodo del look deja de depender solo de que su path diga Look_Files -tambien entra la expresion TCL que resuelve desde root.name- y si queda alguno sin reconocer se avisa en el cartel final. [ HieroTools - Log de los nodos de color en Create NK v000 ]

        - LGA BurnIn: fondos y textos alineados en cualquier posicion y rotacion, y refresh del viewer sin abrir tabs (`LGA_BurnIn.gizmo` v4, `LGA_RoundedPanels.blink` v0.05, `LGA_NKS_BurnIn v1.04`, `Blink v1.05`, `Logic v1.07`, `Panel v2.03`). Lo que se rompia: la caja del Text2 de un campo justificado a la derecha (Resolution, FPS) mandado a la columna izquierda -o al reves- quedaba de ~10 px y el texto se apilaba palabra por palabra mientras el fondo seguia de una linea; el ancla 3x3 escribia la constante cruda en el knob sin considerar la justificacion ni la rotacion y sacaba los campos de cuadro; y por API los campos de metadata miden texto vacio, asi que el pivote de rotacion caia en el borde del panel y el ancla no tenia ancho. Ahora las cajas se anclan por el borde que ubica el texto, el ancla convierte celda a knob segun justificacion y rotacion, la logica recuerda el ultimo ancho medido en render, y un cambio de knob programa un `Viewer.flushCache()` + `resumeCaching()` diferido. Verificado con capturas a 1:1 en NKS. [ HieroTools - BurnIn alineado en toda posicion y refresh sin tabs ]

v3.90:

        - Create NK v000 medía el EditRef con el ffprobe del sistema y fallaba en toda maquina que no lo tuviera (`LGA_NKS_CreateNKScript v1.08`). Se lo invocaba por nombre pelado, confiando en el PATH: en la maquina donde se escribio la tool estaba instalado aparte, asi que el bug no se veia, pero en la de cualquier artista la duracion no se podia medir en NINGUN shot y el rango de review quedaba el del template. Ahora usa el ffprobe que ya viaja en el pack (`LGA_NKS_Shared/FFmpeg_Win`), con ruta armada desde `__file__`; el PATH queda solo de respaldo para macOS, donde todavia no hay binarios propios. El aviso distingue si lo que falta es ffprobe, y el log registra cual se uso. [ HieroTools - ffprobe del pack en Create NK v000 ]

v3.89:

        - Coordination Panel suma el boton Download AMF (`LGA_NKS_Coordination_Panel v1.27`, nuevo `LGA_NKS_FileManagerS3_DownloadAmf v1.00`): sin la carpeta _input/Look_Files bajada del shot, faltan los .amf/.cdl que hacen falta para ver bien los renders de comp con el color correcto, y hasta ahora habia que bajar el shot completo entero o buscarla a mano en disco. El boton nuevo, ubicado entre Download Clip y Reveal in Flow, dispara la descarga de esa carpeta puntual via FileManagerS3 CLI, reusando la deteccion de shot y el launcher que ya usa el boton Download Shot. No chequea si la carpeta ya existe en disco: el punto de este boton es bajarla justamente cuando falta. [ HieroTools - Boton Download AMF en el Coordination Panel ]

        - Create NK v000: el EditRef ya no queda roto, el reveal respeta el explorador del usuario y los problemas se avisan (`LGA_NKS_CreateNKScript v1.07`). El Read del EditRef heredaba la ruta del template con solo el shot cambiado, y como el reemplazo no toca la VERSION, un shot con EditRef_v002 nacia apuntando al v001 y con el Read en error; ahora toma la ruta real del mov. El TimeClip recibe first/last del rango ya colocado (1001 + handle leido del timeline) en vez de la duracion cruda, que es lo que muestra su propio label. El boton del cartel final abre la carpeta con `os.startfile` y ya no fuerza explorer.exe. Y todo lo que puede salir mal -sin CDL, sin LUT, sin AMF, sin publish v000, EditRef no medible, plates sin frames- se junta y se muestra destacado en el cartel. [ HieroTools - Create NK v000 avisa los problemas y arregla el EditRef ]

        - Create NK v000 pregunta antes de pisar un v000 que ya existe (`LGA_NKS_CreateNKScript v1.06`): antes abortaba con un cartel de "no se pisa nada", que dejaba sin salida al caso normal de querer rehacerlo. Ahora pregunta con el cartel del pack, sin opcion recomendada porque pisar es destructivo y no corresponde empujar ninguna de las dos, y si se acepta, el .nk que estaba se conserva como `.nk~` antes de escribir -misma convencion de respaldo que deja Nuke al guardar-, asi que pisar es reversible. La ventana de rango nombra lo que va a pasar: su boton dice Overwrite en vez de Create. Verificado que sin permiso sigue sin pisar y que el respaldo se reemplaza en overwrites sucesivos. [ HieroTools - Create NK v000 pregunta antes de pisar ]

        - Projects Panel: el toggle de contexto invierte el orden visual (`LGA_NKS_UIManager v1.04`): `studio` pasa a la izquierda y `client` a la derecha. Es solo la posicion de los dos `addWidget`; el estado activo lo pinta `_refresh_context_toggle()` eligiendo el boton por IDENTIDAD y no por su lugar en el layout, asi que el resaltado sigue al boton correcto. Verificado renderizando los DOS estados con el harness, que era el unico riesgo del cambio. De paso quedo documentado en `LGA_NKS_Projects_Panel.md` que el INI de contexto lo reescribe el panel en runtime y por eso no se versiona en ningun repo. [ HieroTools - Toggle de contexto con studio a la izquierda ]

        - Apply AMF: la autoseleccion de Hiero ya no se confunde con una seleccion (`LGA_NKS_ApplyAMF v0.90`, `LGA_EditToolsPanel v3.07`): parado sobre un shot y sin tocar nada, `te.selection()` ya devuelve UN item, asi que la tool creia estar respetando una eleccion del usuario y terminaba operando sobre un solo track en vez del shot entero. El umbral pasa a ser por CANTIDAD: con DOS o mas clips seleccionados manda la seleccion; con uno o ninguno se barren todos los tracks bajo el playhead, tomando solo los .exr para no meterle la cadena a un EditRef. Ese filtro no se aplica a la seleccion explicita. El boton toma el atajo Shift+L y Toggle AMF queda comentado como referencia, sin atajo. La autoseleccion quedo documentada en `Docu_Metodos_Seleccion_Clip.md` y el borrado de soft effects en `Docu_SoftEffects_Aprendizajes.md`. [ HieroTools - Apply AMF regla de seleccion y playhead ]

        - Apply AMF pone y saca la cadena de color con un solo boton (`LGA_NKS_ApplyAMF v0.80`, `LGA_EditToolsPanel v3.06`): tener los soft effects colgados en el timeline todo el tiempo estorba. Con el flag `TOGGLE_CREATE_DELETE` el boton decide UNA vez para toda la tanda: si algun clip objetivo ya tiene la cadena la borra de todos, y si ninguno la tiene la crea; decidir por clip crearia en unos y borraria en otros en la misma pasada. Sin seleccion opera sobre los clips bajo el playhead en todos los tracks, como Toggle AMF. El borrado va con `eDontRemoveLinkedItems`: los efectos se crean linkeados al clip y sin esa opcion `removeSubTrackItem` se lleva puesto el CLIP; si la enum no esta se cancela en vez de caer a la version peligrosa. Solo toca lo linkeado o de rango exacto que ademas cargue un archivo de Look_Files, asi no se pierde ni un grade que abarca varios clips ni un efecto de la misma clase puesto a mano. [ HieroTools - Apply AMF pone y saca los soft effects ]

        - LGA_QtAdapter_HieroTools no tenia version en su header (`LGA_QtAdapter_HieroTools v1.00`): los cuatro adapters (ToolPack, ToolPack-B, ToolPack-Layout y HieroTools) son la misma API en cuatro copias, y ninguno llevaba un numero interno: no habia forma de ver de un vistazo si una copia habia quedado atras. Fue exactamente lo que le paso al de ToolPack-B, que perdio el bloque de guardas de widget vivo sin que nadie lo notara hasta que se compararon los cuatro a mano. Se le agrego el header canonico del repo, arrancando en v1.00 con la misma nota en las cuatro copias; el bloque descriptivo largo (lista de consumidores e "Incluye compatibilidad para") se conservo integro. Sin cambios de codigo ejecutable. [ HieroTools - Versionar el header del adapter de Qt ]

        - LGA_UI_Style_HieroTools: `semibold_css()` y `semibold()` vuelven a distinguir el peso 600 del regular (`LGA_UI_Style_HieroTools v1.26`): en Nuke 16 (Qt 6.5+) los tres archivos de Inter reportan la familia "Inter" en vez de que la SemiBold caiga en su propia familia "Inter SemiBold"; `semibold_css()` nombraba esa familia igual, asi que terminaba emitiendo `font-weight: normal` -o sea REGULAR- en todo lo que el diseno pone en 600. Ahora las dos funciones chequean si la familia del semibold coincide con la de la regular: si coincide, piden el peso 600 por numero -el camino que ya existia para cuando las fuentes no cargan- en vez de nombrar una familia que ya no distingue nada. Medido con Style.BTN_PRIMARY: 242 pixeles con tinta antes, 323 despues. [ HieroTools - Corregir semibold_css() y semibold() para Qt 6.5+ ]

v3.88:

        - Flow Push: las notas ahora salen con subject y task (`LGA_NKS_Flow_Push_connector v1.11`): en el Notes page, filtrar por la columna Tasks no mostraba ninguna nota pusheada y todas figuraban sin asunto. La causa son dos campos propios de Note que la web completa desde el cliente y que el push nunca escribia: `Note.tasks` —la columna Tasks, que es un campo aparte y NO se deriva de `note_links`— y `Note.subject`, que no lo genera el servidor. Ahora la Task se toma del `task_id` que el push ya tenia resuelto y el subject replica el formato de la web para links [Shot, Version]. El nombre de pila del autor se resuelve una vez por sesion desde el login autenticado. Las notas viejas se repararon en Flow deduciendo la Task de `Version.sg_task`, metodo validado contra notas creadas desde la web. [ HieroTools - Las notas del push de Flow llevan subject y task ]

        - Shot Info: Shift+Click sobre la imagen de una nota fuerza su redescarga (`LGA_NKS_Flow_Shot_info v1.99`): el sync de PipeSync nunca vuelve a bajar un attachment ya cacheado, asi que una imagen corrupta o incompleta no tenia forma de recuperarse. Ahora el Shift+Click corre `refresh_note_attachments.py` de la instalacion de PipeSync en un `QRunnable`, sin bloquear la UI de Hiero, y repuebla los thumbnails de esa nota. Lo que costo dos rondas de auditoria: la DB destino se fija con `PIPESYNC_CACHE_DIR` segun el contexto —sin eso, en Client el script resolvia la base de Studio, donde ese mismo `--note-id` es OTRA nota y le pisaba los attachments en silencio—, el parseo exige que la linea sea un dict (un `AttributeError` adentro de un slot en cola puede tirar Hiero entero) y `no_attachments` repuebla vacio en vez de dejar imagenes fantasma que la DB ya no referencia. En macOS queda INERTE a proposito: no hay raiz de instalacion confirmada y adivinarla arriesga escribir en la base equivocada. [ HieroTools - Shift+Click en una imagen de nota fuerza la redescarga de sus attachments ]

        - LGA BurnIn: la fuente Inter por fin llega al RENDER (`LGA_HieroTools_Startup`, `LGA_NKS_BurnIn_Logic v1.06`): Nuke parte `NUKE_FONT_PATH` por ":" (estilo Unix) incluso en Windows, asi que la letra de unidad ("C:") rompia el path, `getFonts` registraba rutas relativas invalidas y el burn-in renderizaba EN SILENCIO con la fuente default — de ahi los textos descentrados verticalmente en el panel y el flip 180 fuera de cuadro que detecto la auditoria. El startup ahora exporta el path SIN letra de unidad (valido en Windows contra el drive del proceso) y la logica valida que el path de getFonts exista antes de medir con el (si no, cae al TTF del repo). Re-validado con la bateria de 9 casos (4 rotaciones x 3 justificaciones) con render fresco por caso: centrado y padding correctos en todos, incluido 180. [ HieroTools - BurnIn fix del font path de Nuke ]

        - LGA BurnIn: validacion end-to-end medida y fixes del pivote y del panel (`LGA_NKS_BurnIn_Blink v1.04`, `LGA_NKS_BurnIn_Panel v2.02`, gizmo regenerado): el pivote de la rotacion pasa a knobs literales del gizmo (`bi_<f>_ax/ay`) escritos por `apply_rotation()` y leidos por el kernel — una sola fuente para texto y fondo; antes cada uno lo calculaba por su lado y podian divergir (el "fondos en cualquier lado"). El ancla 3x3 y los sliders X/Y/Size re-sincronizan ese pivote al mover un campo rotado. Dos bugs del paint-toggle medidos con drags reales: pintaba el estado viejo (QCheckBox togglea al release, no al press) y salteaba checkboxes en drags rapidos (Qt agrupa moves; ahora se interpola la linea). Radio de esquinas default 11. Verificado con bateria automatizada en NKS: 9 casos de padding medidos con capturas (4 rotaciones x 3 justificaciones) y clicks/drags reales sobre el panel (anclas, rotacion, multi-seleccion, drag-sliders, pintado). [ HieroTools - BurnIn validacion medida y fixes de pivote y panel ]

        - LGA BurnIn: rotacion por campo funcionando (`LGA_NKS_BurnIn_Blink v1.03`, `LGA_NKS_BurnIn_Logic v1.05`, `LGA_NKS_BurnIn v1.03`, `LGA_NKS_BurnIn_Panel v2.01`): cada campo (texto Y su panel de fondo) rota 0/90/180/270 desde la ventana, aplicado a la seleccion de filas. El mecanismo salio de rotar un Text2 a mano en la UI (tab Groups): el transform del grupo "root transform" SI renderiza en soft effects, con la rotacion como LITERAL en la posicion [10] de `animation_layers` y el grupo seleccionado (el transform tab del nodo y las expresiones en el blob no renderizan; medido). `apply_rotation()` escribe ese literal con pivote en el centro del panel, el mismo que se bindea al kernel, asi texto y fondo giran juntos. El formato real sale de la secuencia activa (por API el gizmo miente 640x480). Validado en NKS a 90 y 270 con capturas. [ HieroTools - BurnIn rotacion por campo ]

        - LGA BurnIn: rediseno de campos, fuente y panel (`LGA_NKS_BurnIn_Logic v1.04`, `LGA_NKS_BurnIn_Panel v2.00`, `LGA_NKS_BurnIn_Blink v1.02`, `LGA_NKS_BurnIn v1.02`, gizmo regenerado): se suman dos campos de texto libre (custom1/custom2) a los seis de metadata, tamano de fuente por campo y selector de peso de Inter. La fuente se lee del repo via `NUKE_FONT_PATH` seteado en el startup, sin instalar nada en la maquina del usuario; con eso el peso Bold —que el Inter de sistema no trae— funciona, y la medicion del ancho del panel pasa del AFM de Utopia al TTF real de Inter. El properties del gizmo queda pelado a un solo boton y todo se edita en la ventana propia, rediseniada a tabla + detalle con drag-sliders estilo Nuke, paint-toggle de checkboxes y seleccion multiple de filas. La rotacion por campo NO se cablea todavia (sin control en el panel), pero se encontro el mecanismo: se rota el grupo "root transform" del Text2 (seleccionarlo y escribir la rotacion como LITERAL en la posicion [10] de `animation_layers`; el transform tab del Text2 y las expresiones en el blob no renderizan; medido). Queda para una pasada de wiring (write del literal + centro por campo). [ HieroTools - BurnIn rediseno campos, fuente y panel ]

        - LGA BurnIn: los fondos ahora abrazan bien el texto y el colorspace se refresca (`LGA_NKS_BurnIn_Logic v1.03`): el ancho del panel se medía con QFontMetrics, pero en NKS Qt no encuentra la fuente "Utopia" (es de Nuke, no del sistema) y la sustituía por una más ancha, dejando siempre sobrante del lado opuesto al ancla. Ahora se mide con las métricas AFM reales de Nuke (`plugins/fonts/UtopiaRegular.afm`), que dan el ancho exacto del render. La normalización de dígitos queda solo en frame/tc (a '0', para que su panel no respire); clip, resolución, fps y colorspace se miden exactos. Además el colorspace se cacheaba para siempre y no reflejaba un cambio manual (ningún evento lo dispara): pasa a un cache con TTL corto. El dev-link de desarrollo se movió a `+Building_Blocks`. [ HieroTools - BurnIn fondos ajustados y colorspace ]

        - LGA BurnIn: paneles que abrazan al texto, layout por anclas y presets (`LGA_NKS_BurnIn_Logic v1.02`, `LGA_NKS_BurnIn_Blink v1.01`, `LGA_NKS_BurnIn_Panel v1.01`, `LGA_NKS_BurnIn_Config v1.02`, gizmo regenerado): los fondos fijos que sobraban por todos lados se reemplazan por paneles medidos del texto real (QFontMetrics, digitos normalizados para que frame/TC no respiren) con un ancla por campo; se elimina la segunda fuente. La ventana suma la posicion de cada campo en % del formato y presets con nombre guardables en AppData, y el properties panel queda compacto (campo y su bg en la misma fila). Dos fixes medidos en NKS: `hiero/project` llega vacio en el stream del timeline (el nombre del proyecto ahora cae a la secuencia activa) y las expresiones python de color no re-evaluan hasta ensuciar el nodo (la ventana nudgea tras cada guardado). Validado en NKS real con capturas. [ HieroTools - BurnIn paneles medidos y presets ]

        - LGA BurnIn definitivo con paneles redondeados por BlinkScript (`LGA_NKS_BurnIn v1.01`, `LGA_BurnIn.gizmo` generado, kernel `LGA_RoundedPanels.blink`, `LGA_NKS_BurnIn_Blink v1.00`, `LGA_NKS_BurnIn_Panel v1.00`): la version solo-Text2 pasa a llamarse LGA BurnIn v0 y el efecto nuevo suma un BlinkScript propio que dibuja los fondos de los seis campos en una pasada GPU, con radio, color y opacidad configurables — lo que el fondo nativo de Text2 no puede. El kernel se compila al instanciarse via kernelSourceFile + Load (recompile por script no funciona en el timeline) y sus parametros se atan por expresion a los knobs de Layout del gizmo. Frame y TC quedan como campos separados, se suma colorspace (por API con cache), fuente aparte para digitos, y un layout default de dos filas balanceadas. Ventana propia con el estilo del pack (Open LGA Panel) para editar el efecto y guardar los objetivos res/fps en el tag del proyecto o como default de usuario. [ HieroTools - LGA BurnIn con paneles BlinkScript ]

        - Nueva herramienta LGA BurnIn (`LGA_NKS_BurnIn v1.00`, `LGA_BurnIn.gizmo`): un solo soft effect en el menu Effects reemplaza la pila de Texts de metadata. Seis campos con checkbox (clip, resolucion, frame, TC, colorspace, fps), posiciones en fracciones del formato en la pestana Layout, fondo por campo y fuente aparte para digitos. Resolucion y fps se pintan de rojo si no coinciden con el objetivo del proyecto: config de usuario en AppData (`BurnIn.json`, no versionado) mas un override que viaja dentro del `.hrox` en un tag del proyecto, que Clean Project no toca. La logica vive en Python (`LGA_NKS_BurnIn_Logic v1.00`) y las expresiones del gizmo la llaman por frame. El gizmo copia la anatomia interna del BurnIn nativo: un Text2 escrito sin sus blobs de estado se dibuja en el origen. Lo aprendido quedo en `Docu_SoftEffects_Aprendizajes.md` y la tool en `Docu_LGA_BurnIn.md`. [ HieroTools - LGA BurnIn soft effect ]

        - Ningun script imprime debug en la consola de Nuke (`LGA_NKS_ApplyAMF v0.61`, `LGA_NKS_ToggleAMF v1.11`, `LGA_NKS_Flow_Push_connector v1.10`, `LGA_NKS_ReviewPic v1.23`, `LGA_NKS_Clip_DisableEXR v1.33`, `LGA_NKS_ScrollTo_TopTrack v1.02`): estos seis son los unicos que quedaban sin logging a archivo, asi que su `debug_print` iba derecho a `print()` y ensuciaba la consola en cada corrida. Pasan a `DEBUG = False`. En los que SI escriben a `.log` no se toco nada: ahi `DEBUG` es el master del log y apagarlo hubiera matado tambien el archivo; ya venian con `DEBUG_CONSOLE = False` y `DEBUG_LOG = True`, que es la combinacion que pide la regla del repo. `LGA_NKS_Flow_Pull` queda igual: su consola ya sale por la variable de entorno `LGA_DEBUG_CONSOLE`, apagada por default. [ HieroTools - Debug por consola apagado ]

        - El boton Viewer | 2.35:1 pasa a 3:2 y los burn-ins se corren solos (`LGA_ViewerPanel v1.74`, `LGA_NKS_Viewer_Mask v1.32`, `LGA_NKS_Timeline_Refresh v1.22`): 3:2 es mas angosto que el 16:9 de la secuencia, asi que la mascara deja barras a los costados y el ajuste viejo -subir en Y el primer efecto llamado Frame- no servia. Ahora se achica la caja de TODOS los efectos del track BurnIn, `x` y `r` hacia adentro por el ancho de la barra, que sale del format real: el texto de la izquierda se corre a la derecha, el de la derecha a la izquierda y el centrado se queda, sin saber cuantos hay ni como se llaman. La opacity, que marca que el burn-in esta corrido, ahora guarda tambien el EJE; con dos en juego un solo valor deshacia el desplazamiento equivocado. El boton 2:1 sale del panel; `viewer_21` y `viewer_235` quedan sin llamar. [ HieroTools - Viewer 3:2 y burn-ins fuera de la mascara ]

        - Nuevo boton Toggle AMF (`LGA_NKS_ToggleAMF v1.00`, `LGA_EditToolsPanel v3.05`): prende y apaga la cadena de color que puso Apply AMF en los clips seleccionados. Es un toggle del CONJUNTO: si queda alguno encendido los apaga todos, y si estan todos apagados los enciende; invertir cada efecto por separado dejaria la cadena mezclada, que es lo que no se quiere ver en un plate. `EffectTrackItem.setEnabled()` no figura en la API documentada de Nuke 16 -ni SubTrackItem ni TrackItemBase la listan- pero existe y es la via buena: al llamarla el knob `disable` del nodo se mueve solo, asi que no hace falta escribirlo por atras. Se deduplica por `guid()`, porque con el clip y sus efectos seleccionados a la vez el mismo efecto llega por dos caminos y el toggle se aplicaria dos veces. La exploracion que lo midio queda en `+Building_Blocks/Hiero/Timeline/LGA_H-Explore_ToggleSoftEffect.py`. [ HieroTools - Boton Toggle AMF ]

        - Create NK v000: ventanas que no bloquean Hiero y arreglos del script generado (`LGA_NKS_CreateNKScript v1.05`). Las dos ventanas eran modales y se quedaban con el control de la app; ahora son no-modales y el flujo sigue por callbacks, como Create EXR v000. La caja del handle se agranda (`HANDLE_SPIN_WIDTH`/`HEIGHT`, a mano: el spinbox va nativo) y muestra al lado el total que da la cuenta. El cartel final trae la ruta coloreada y un boton Show in Explorer / Show in Finder junto al OK marcado. En el script generado se borra el StickyNote `SCRIPT BASE_v` del template, y se dejan de arrastrar los dos stamps de aPlate/aDenoised que viven fuera de la columna de input (junto al CopyMetaData y adentro del backdrop Regrain): el recomputo de columnas ahora mueve solo los nodos que estaban en la x del Read. [ HieroTools - Create NK v000 no-modal y stamps en su lugar ]

        - Las ventanas llevan la fuente del pack (`apply_ui_font` en 15 archivos, `LGA_NKS_Flow_Shot_info v1.98`): ninguna ventana de HieroTools llamaba al helper, asi que todas se dibujaban con la fuente del host en vez de con la Inter que viaja en el release, y el peso 600 de las hojas caia en una negrita sintetizada. Se suman treinta llamadas al final del armado de las ventanas y carteles -Wasabi, Flow Create/Update/CheckShots, asignados, OpenInNukeX, Push, cola de transcode, TaskMismatch, Shot Info y la franja Task history- y de nuevo donde se agregan widgets mas tarde: filas de task, thumbnails arrastrados y tablas que se redibujan. Donde habia medidas tomadas antes de la fuente -columnas de TaskMismatch, elidido de chips y nodos- ahora se remiden despues. Queda afuera `LGA_NKS_Edit_Panel`, que es un panel dockeado todavia sin migrar al modulo. Capturas antes/despues regeneradas. [ HieroTools - Fuente del pack en todas las ventanas ]

        - Create NK v000 y el selector de task a las hojas del modulo (`LGA_NKS_CreateNKScript v1.04`, `LGA_NKS_TaskSelectionDialog v1.44`): las dos tools tomaban `Color`/`Metric` pero rehacian a mano las hojas que el modulo ya trae, asi que Create NK v000 salia visiblemente distinta de su hermana Create EXR v000 sin tener un solo hex propio. Las dos ventanas de Create NK v000 y el dialogo de task pasan a `Style.FORM`, `Style.BTN_SECONDARY` para las opciones y Cancel, y `Style.BTN_PRIMARY` para Create -unico violeta y ultimo a la derecha-; se van los diez `font-size` propios, el spinbox del handle vuelve a ser nativo como manda la hoja y las dos suman `apply_ui_font`, que les faltaba. Los colores de task del selector quedan: son data. Auditados sin cambio `LGA_NKS_Flow_Shot_info` y `LGA_import_shots_bulk`: el primero no tiene un solo control estandar -su UI son QLabel y QWidget a medida-, y el segundo solo le agrega a un QFileDialog del sistema una regla de seleccion para la que el modulo no tiene hoja. Queda anotado que Shot_info conserva `font-size` a mano y un bloque propio de QScrollBar, que si tiene hoja (`Style.SCROLLBAR`): es deuda a mirar aparte, no la razon por la que no se migro. Capturas antes/despues regeneradas. [ HieroTools - Create NK v000 y selector de task a las hojas del modulo ]

        - Pestanas al mismo tono que su panel (`LGA_NKS_CreateV000 v1.16`, `LGA_import_shots v1.39`): en las dos ventanas con tabs la pestana ACTIVA salia en `SURFACE_HEADER` (#2B2B2B), dos tonos mas clara que el panel que abre (#212121), y se leia como una caja apoyada encima en vez de como su continuacion. Cada tool tenia su hoja de tabs copiada; las dos pasan a `Style.TABS` del modulo (v1.24), donde la activa es `WINDOW` y las inactivas `FIELD_BG`. Los `QColor` que `_HeaderSeparator` y `_ImportShotTabBar` usan para pintar a mano el hueco y los separadores replicaban el color viejo a mano: se repuntan a `BORDER_STRONG` y `WINDOW` para no quedar desalineados del QSS. Las tres ventanas suman `apply_ui_font`, que les faltaba. Capturas regeneradas y verificadas pixel a pixel. [ HieroTools - Pestanas al tono del panel ]

        - El shotname va INCLUIDO en el color de parte comun (`LGA_UI_Style_HieroTools v1.23`): en v1.22 el shot arrancaba la paleta y salia de otro color que la raiz que lo precede, asi que la ruta seguia leyendose partida. Ahora todo hasta el shot inclusive va en `PATH_COMMON` y la paleta arranca en el segmento siguiente. Mismo cambio en las cuatro copias del modulo de estilo. La regla se reescribio en `Docu_UI_Style.md` y en los AGENTS.md de los nueve repos porque estaba redactada de forma ambigua ("los segmentos anteriores al shot"), que fue lo que hizo que se implementara al reves. [ HieroTools - Shotname incluido en el color comun ]

        - Los paths coloreados se anclan en el shotname (`LGA_UI_Style_HieroTools v1.22`): cuando un segmento de la ruta es un nombre de shot, todo lo anterior va en un solo color (el de parte comun) y la paleta por nivel arranca recien en el shot; sin shot detectable se sigue coloreando por nivel desde la raiz. Mismo cambio en las cuatro copias del modulo de estilo; la regla quedo en `Docu_UI_Style.md` de LGA_ToolPack y `colorize_path_pair` mas la revision de ventanas quedan en `ROADMAP.md`. [ HieroTools - Paths anclados al shotname ]

        - Nuevo boton Create NK v000 en el Edit Panel (`LGA_NKS_CreateNKScript v1.03`, `LGA_EditToolsPanel v3.04`; Create v000 pasa a llamarse Create EXR v000): crea el comp v000 del shot activo editando como texto el template .nk del proyecto (`<raiz>/ASSETS`). Resuelve plates y denoised reales (version mas alta con EXR, con fallback si la mas alta esta vacia), borra los trios Read+Anchor+Stamp de plates inexistentes, clona trios para plates extra (gPlate, segundos cbPlate) reacomodando columnas y backdrop, apunta los OCIO del output y del grupo VIEWER_INPUT al CDL/CLF de Look_Files, y centra el EditRef con su duracion real (ffprobe) o el offset del timeline. Rango siempre desde 1001, con el publish comp_v000 como default si existe, duraciones del timeline cuando hay retime, y el rango resultante en vivo. Escribe `Comp/1_projects/<shot>_comp_v000.nk` sin pisar nada; log de cada corrida a `logs/`. [ HieroTools - Boton Create NK v000 ]

        - Import Shot: fin de la migracion de estilo (`LGA_import_shots v1.38`): quedaba QSS suelto con hexes fuera de la pasada v1.36 —spinboxes y combos con flecha propia, QSS de rename/transcode, labels, log, barra de progreso y header de la preview— y pasa a tokens y hojas del modulo (LINE_EDIT, BTN_ICON). El header seq/shot usa INFO/ENTITY, mismo criterio que CreateV000 y la cola de transcode. Quedan como data, igual que en las hermanas: la paleta de la preview y los colores por tipo/estado, el hover/pressed del link de shot (sin tokens para esos matices), el ambar del footer de omitidos y la banda gris de secciones. Capturas regeneradas y verificadas. [ HieroTools - Import Shot sin QSS suelto ]

        - Checkboxes: celdas sin recorte y aire en los rotulados (`LGA_import_shots v1.37`, `LGA_NKS_Wasabi_PolicyUnassign_CompletedShots v1.04`, `LGA_NKS_Flow_Pull v3.61`, `LGA_NKS_Flow_Push v4.13`, `LGA_NKS_TaskMismatchDialog v1.04`): los checkboxes de celda salian recortados porque el padding horizontal de item de Style.TABLE (6 px por lado) achica el rect del cell widget; las columnas de checkbox de las tablas de Import Shot pasan de 28 a 34 px y la de CompletedShots deja ResizeToContents por 34 px fijos. Tres checkboxes con texto sin `lgaLabeled` (keep on top de Pull y TaskMismatch, borrar imagenes de Push) quedaban con el texto pegado al cuadrito y suman la propiedad. Capturas regeneradas con el modulo v1.21; CreateV000 verificado sin fix propio: pinta fondo y checkboxes de tabla correctos. Ademas el modulo de estilo pasa a v1.21 en las CUATRO copias: Style.TABLE incluye el bloque del checkbox, porque la hoja propia de una tabla cortaba la herencia de la regla QCheckBox y el fondo del cuadrito caia al palette del host. Y el cartel de verificacion de version del Push (v4.12) deja de responder a Enter: Cancelar era el default pero sin marca visual; ESC cancela. [ HieroTools - Checkboxes de celda sin recorte y lgaLabeled faltantes ]

        - Import Shot, cola de transcode y Create v000 al estilo del pack (`LGA_import_shots v1.36`, `LGA_import_shots_transcode_queue_ui v0.03`, `LGA_NKS_CreateV000 v1.15`): las ventanas mas grandes del Edit Panel dejan sus bloques _DIALOG_STYLE/_BTN_*/_TAB_STYLE locales y pasan a las hojas del modulo, con las tabs armadas con tokens. Los colores de estado de la cola coincidian exacto con OK/WARNING/ERROR y pasaron al token; el pending azul difiere de INFO y queda como data, igual que la paleta de la preview, los colores por task y el teal de la cola. Dos arreglos que pedia FORM: los QRadioButton quedaban ilegibles y los separadores VLine aplastados; se agregan _RADIO_STYLE y la geometria del separador con tokens. Los checkboxes de celda quedan al indicador del pack. En import_shots queda QSS suelto menor (spinners, combos) para una pasada futura. [ HieroTools - Ventanas grandes del Edit Panel al estilo del pack ]

        - Comparadores y dialogos de Import Shot al estilo del pack (`LGA_NKS_CompareEXR_to_aPlate v1.16`, `LGA_NKS_CompareVerToEditref v1.19`, `LGA_NKS_MatchVerToEXR v0.83`, `LGA_import_shots_settings v1.03`, `LGA_import_shots_rename_settings v1.02`, `LGA_import_shots_transcode v1.01`, `LGA_import_shots_bulk v1.03`): las tres GUIs de comparacion dejan el tema claro del host y pasan a tokens, sin Style.TABLE porque las celdas de estado las pinta el delegado y esos colores son data —solo el verde de match paso a OK_BG, que coincide exacto—. Los dialogos de guardar preset y el aviso de overwrite pierden sus bloques _BTN_* locales; el ambar del titulo queda como identidad en los presets y pasa a WARNING_TEXT en el overwrite, donde si es advertencia. La seleccion del browser de carpetas pasa a la del pack. [ HieroTools - Comparadores e Import Shot al estilo del pack ]

        - Flow Panel al estilo del pack (`LGA_NKS_Flow_Push v4.11`, `LGA_NKS_Flow_Pull v3.60`, `LGA_NKS_Flow_Shot_info v1.97`, `LGA_NKS_MessageBox v1.01`): las ventanas grandes del panel —dialogo de notas del push, seleccion de versiones, resultados del pull y Shot Info con su hoja propia entera— pasan a tokens. Los question() pasan a ask_question() conservando el default No de los dos peligrosos; los carteles del MessageBoxManager siguen no-modales. La tabla del Pull no lleva Style.TABLE porque sus filas las pinta el delegado con colores de estado, que quedan intactos igual que los colores de notas y playlists de Shot Info. El helper de carteles suma apply_ui_font, sin el cual los carteles salian con la fuente del host. Queda para decidir: PushVersionDialog conserva Enter en Cancelar sin marca visual, tension vieja con la regla del boton marcado. [ HieroTools - Flow Panel al estilo del pack ]

        - Ventanas Wasabi y de Flow al estilo del pack (`LGA_NKS_Wasabi_PolicyAssign v1.01`, `LGA_NKS_Wasabi_PolicyUnassign v0.62`, `LGA_NKS_Wasabi_PolicyUnassign_CompletedShots v1.03`, `LGA_NKS_Flow_CreateShot v1.48`, `LGA_NKS_Flow_UpdateThumb v1.03`): seis ventanas con QSS y hexes propios pasan a tokens. Criterios que fijan precedente: la cruz de quitar por fila va en BTN_ICON y no en violeta —sin accion global no hay primario—; el Take Snapshot del form de shot baja a secundario porque el unico violeta es Create/Modify; la tabla de completados suma el fondo alternado por token, que Style.TABLE no trae. La paleta de estados de Flow, los colores por task y el color de usuario de la DB quedan intactos: son data, no estilo. Capturas antes/despues y auditoria del diff con data verificada byte a byte. [ HieroTools - Ventanas Wasabi y Flow al estilo del pack ]

        - Dialogos chicos al estilo del pack (`LGA_NKS_OpenInNukeX v1.32`, `LGA_Contact_Sheet_OpenInNukeX v0.04`, `LGA_NKS_Flow_CheckTimelineShots v1.03`, `LGA_NKS_GetClip v1.87`, `LGA_NKS_Clip_DisableEXR v1.32`, `LGA_NKS_Reconnect v1.20`, `LGA_NKS_mediaMissingFrames v1.52`, `z_version_everywhere v1.01`): diez dialogos que salian con el tema del host o con hexes propios pasan al modulo de estilo y al helper de carteles. Los botones de accion quedan unicos, violetas y a la derecha; los hexes de HTML pasan a tokens por rol (WARNING_TEXT, OK_TEXT, INFO, TEXT_DIM). El question de DisableEXR pasa a ask_question() con retorno equivalente. Quedan a proposito los dos colores de clip por tipo de pista de Reconnect, que son data. Verificado con capturas antes/despues renderizadas fuera de Hiero y auditoria independiente del diff. [ HieroTools - Dialogos chicos al estilo del pack ]

        - Modulo de estilo al dia (`LGA_UI_Style_HieroTools v1.20`): la copia de HieroTools habia quedado en v1.06 mientras las tres del ToolPack ya estaban sincronizadas en v1.20, asi que "unificar" contra ella era unificar contra un estandar viejo. Se adopta el cuerpo v1.20 completo —temas, fuentes del pack (Inter y JetBrains Mono, ahora en `LGA_NKS_Shared/fonts/`), tokens nuevos, `apply_ui_font()` y los helpers semibold— con los dos renombres de rigor (modulo y adapter), verificado byte a byte contra la copia del ToolPack-B. No-regresion medida con capturas A/B renderizadas fuera de Hiero: las ventanas ya migradas quedan visualmente iguales salvo el ajuste tipografico propio del v1.20. [ HieroTools - Modulo de estilo v1.20 con temas y fuentes ]

        - Carteles con el estilo del pack (`LGA_NKS_MessageBox v1.00` + 19 paneles y scripts): los avisos de error y de informacion salian con el tema del host, porque los `QMessageBox` estaticos de Qt no pasan por ninguna hoja de estilo de HieroTools. Nuevo helper en `LGA_NKS_Shared` con firmas espejo (`show_info`/`show_warning`/`show_error`/`ask_question`) estilado con `LGA_UI_Style_HieroTools`, y migracion mecanica de 130 callsites en 19 archivos. `ask_question` no usa `QMessageBox`: en Windows su boton afirmativo cae a la IZQUIERDA y la regla del pack pide la accion ultima a la derecha; se arma como QDialog con layout manual. Los `question()` y carteles a medida quedaron afuera a proposito. Verificado renderizando los carteles con el PySide6 de Nuke sin abrir Hiero, y auditado contra el diff completo. [ HieroTools - Carteles de aviso con el estilo del pack ]

        - Apply AMF no duplica efectos (`LGA_NKS_ApplyAMF v0.50`): apretar el boton dos veces sobre el mismo clip apilaba otro OCIOCDLTransform y otro OCIOFileTransform encima de los que ya estaban. Ahora se barre que hay sobre el clip antes de tocar nada y se crea solo lo que falta: si ya tiene los dos no se hace nada, si tiene uno se agrega el otro. El barrido no se queda con `linkedItems()`, porque ahi solo figuran los efectos creados con `createEffect(trackItem=...)`: un efecto arrastrado a mano en la UI vive suelto en un subtrack y afecta al clip por solaparse en tiempo, sin que el clip sepa nada de el. Se miran los dos lugares y se deduplica por `guid()`. Un efecto que solapa parcial se reporta pero no bloquea, porque puede ser un grade que abarca varios clips del track. [ HieroTools - Apply AMF crea solo los efectos que faltan ]

        - Nueva tool Apply AMF (`LGA_NKS_ApplyAMF v0.40`, `LGA_EditToolsPanel v3.03`): un boton en el Edit Panel que le arma a los clips seleccionados la cadena de color del shot. Del media del clip se resuelve la carpeta del shot con `extract_shot_code_from_path()` —valida el vendor code contra la DB de PipeSync, asi que sirve igual desde `_input` que desde un publish de Comp— y se baja a `_input/Look_Files`. Ahi manda el `.amf`: sus `lookTransform` dicen cuales aplicar (los `applied="false"`; el IDT y el Reference Gamut Compress ya vienen en el plate), en que orden y con que parametros. De ahi salen el `.cdl` para un OCIOCDLTransform —con el `cccid` leido del XML y el working space ACEScct que pide el `cdlWorkingSpace`, no el `scene_linear` del nodo— y el `.clf` para un OCIOFileTransform. Los efectos se crean linkeados al clip con `createEffect(trackItem=...)`. [ HieroTools - Nueva tool Apply AMF ]

        - Push (`LGA_NKS_Flow_Push_connector v1.09`): las notas del push se colgaban de la Version equivocada, y en la que el usuario miraba no aparecia nada. Cuando en Flow conviven dos convenciones de naming que solo difieren en el orden del vendor code (`PROJA_1013_0800_comp_VND_v003` y `PROJA_1013_0800_VND_comp_v003`), las dos contienen `_comp_` y las dos son v003, asi que el filtro por token y numero matchea las dos y los resolvers se quedaban con `matching_versions[0]`: siempre la del ID mas bajo. El estado en cambio se escribia en TODAS las coincidencias, asi que se veia bien en la Version correcta y tapaba el problema. Ahora gana la Version cuyo code coincide exacto con el stem del filename del clip; si quedan varias sin match exacto se elige una pero se avisa por `warnings`. `update_version_status` se acota al mismo nombre. Solo se manifestaba donde hay Versions duplicadas por shot. [ Flow Push - Desempatar la Version destino por nombre ]

        - Push (`LGA_NKS_Flow_Push v4.09`, `LGA_NKS_Flow_Push_connector v1.08`, `LGA_NKS_Flow_Status_Config v1.02`): pushear un shot a Rev Hold cambiaba la task en Flow pero perdia la nota, sin ningun error visible. La lista de estados que llevan nota estaba hardcodeada en cinco lugares y se desincronizo: `revhld` figuraba en las cuatro de Flow_Push —por eso el dialogo si pedia la nota— pero no en la del conector, que descartaba el mensaje y devolvia `success` con `applied.note` en False y `warnings` vacio. `revprd` (Rev Prod, solo client) no estaba en ninguna de las cinco. Se borran las cinco copias: ahora salen de `NOTE_CAPABLE_CODES` via `is_note_capable()`, el mismo movimiento que la v1.05 del conector ya habia hecho con `status_translation` y por el mismo motivo. Los dos estados pasan a crear nota y a mandar la Version a `vwd`. [ Flow Push - Los estados con nota salen de una sola lista ]

        - Push (`LGA_NKS_Flow_Push v4.08`, `LGA_NKS_Flow_Push_connector v1.07`): el diálogo de notas acepta media arrastrada. Al arrastrar png/jpg sobre la ventana, ésta se tapa con el cartel "Drop to add media" en violeta y borde punteado; al soltar, los thumbs se suman debajo de las capturas de ReviewPic. Esa media viaja al conector aparte (`extra_images`) y la sube `attach_files_to_note` con su nombre original: la convención `annot_version_<id>.<frame>` es para anotaciones de un frame y una referencia suelta no lo es. El checkbox de borrado y la × de cada thumb siguen borrando solo el cache de ReviewPic; la media arrastrada nunca se toca en disco, su × solo la saca del mensaje. La nota se crea aunque el mensaje quede vacío si hay imágenes, y un attach parcial ahora se avisa por ventana en vez de quedar solo en el log. [ Flow Push - Adjuntar media arrastrada a la nota ]

        - `LGA_NKS_FrameNumber_Create.py` no parseaba: el docstring de cabecera cerraba con `"""import hiero.core` pegado en la misma linea, asi que el modulo tiraba `SyntaxError` y `LGA_NKS_FrameNumber.py` no podia importarlo. La tool Frame Number estaba rota. Se le devuelve el salto de linea. Venia del commit de renormalizacion de finales de linea de agosto. [ HieroTools - Arreglar el SyntaxError de FrameNumber_Create ]

        - Barridos de ventanas ajenas (`LGA_import_shots v1.34`, `LGA_import_shots_transcode_queue_ui v0.02`, `LGA_NKS_CreateV000 v1.13`, `LGA_Projects_Panel_SwitchSequence v2.33`): quedaban siete lugares recorriendo `QApplication.topLevelWidgets()` a pelo para buscar un dialogo por `objectName`. Es el mismo tipo de llamada que `allWidgets()`, con un conjunto mucho mas chico: materializa un wrapper de PySide por cada ventana de golpe. Los siete pasan a `iter_live_widgets(only_top_level=True)` con `safe_widget_call`, que unifica el barrido con el resto del pack y saltea los wrappers ya invalidados sin depender del `try/except` de cada lectura. Los dos cleanups agresivos del Switch Sequence revalidan con `is_widget_alive()` antes de cada `deleteLater()`, por consistencia con las otras dos funciones del archivo. [ HieroTools - Unificar el barrido de ventanas en iter_live_widgets ]

        - Widgets ya destruidos (`LGA_Projects_Panel_SwitchSequence v2.32`, `LGA_NKS_CheckProjectVersions v1.84`, `LGA_NKS_Timeline_Refresh_Wrap v1.43`): `QApplication.allWidgets()` materializa de una sola vez un wrapper de PySide por cada widget del proceso. Si Hiero esta creando o destruyendo widgets en ese momento, la llamada corrompe el heap y el proceso muere con `0xc0000374`, o mas tarde con un access violation adentro de `QWidget::~QWidget` durante un garbage collect. Pasa igual con Qt5 y con Qt6. El adapter suma cuatro helpers —`is_widget_alive`, `safe_widget_call`, `iter_live_widgets` e `iter_live_children`—. `iter_live_widgets` no envuelve a `allWidgets()`: baja desde `topLevelWidgets()`, que cubre el mismo conjunto sin la llamada que rompe. Los tres scripts pasan a usarlo, y se revalida cada widget antes de su `deleteLater()`. [ HieroTools - Dejar de llamar allWidgets al barrer widgets ]

v3.87:

        - Task CG en contexto client (`LGA_NKS_GetClip v1.85`, `LGA_NKS_Flow_NamingUtils v1.16`, `LGA_NKS_TaskSelectionDialog v1.43`, `LGA_NKS_Flow_Pull v3.58`, `LGA_NKS_Flow_Push v4.07`, `LGA_NKS_CreateV000 v1.12`, `LGA_NKS_Flow_Task_Config v1.26`): en client ahora existen dos tasks, comp y CG; la CG agrupa las entregas 3D de los vendors con la disciplina en el filename (layout, lighting...). Se suma el track `_cg_` (puede haber varios, uno por disciplina) a las listas centrales, y `normalize_task_name()` mapea por exclusión toda task no registrada a `cg`, solo en client — en studio no cambia nada. Pull procesa los clips del track `_cg_` aunque el filename no tenga token de task conocido (los demás tracks conservan el filtro histórico), compara la versión más alta por stream usando el `version_code` de la DB de PipeSync, y avisa con un diálogo si la DB tiene disciplinas de CG sin clip en ningún track `_cg_`. Push discrimina por stream al actualizar la DB local. Create v000 ofrece comp y cg en client, con el color de cleanup. [ Client - Task CG con track propio en el timeline ]

        - Flow Pull (`LGA_NKS_Flow_Pull v3.57`, `LGA_NKS_Flow_NamingUtils v1.15`): en los shots cuyo nombre termina con un vendor code configurado en PipeSync, el parser reconocía bien ese vendor pero trataba los bloques del publish como una descripción propia del shot. Así intentaba buscar `PROJA_1013_0800_VEN_PLATE_comp` cuando la DB guarda `PROJA_1013_0800_VEN`. Pull pasa a tomar primero la carpeta de shot de la ruta, validada contra los vendors de `project_settings_cache`; si no hay carpeta reconocible conserva el parser histórico. Además el proyecto se compara sin distinguir mayúsculas/minúsculas, porque `VFX-proja` y el valor cacheado pueden diferir solo en case. [ Flow Pull - Resolver shots con vendor desde la carpeta ]

        - PipeSync Paths (`LGA_NKS_PipeSyncPaths v1.02`, `LGA_NKS_ContextProfile v1.00`): en Windows, el Pull buscaba la DB client en `C:/Portable/LGA/PipeSync/cacheClient`, pero la instalación client la guarda en `C:/Portable/LGA/PipeSync_Client/cacheClient`. Se corrigen los dos resolvers para que Flow Pull, Flow Push y los demás consumidores de PipeSync lean la cache del perfil correcto. [ PipeSync - Corregir ruta de cache client ]

        - Wasabi Policy Assign (`LGA_NKS_Wasabi_PolicyAssign v1.00`, `wasabi_policy_utils v0.96`, `LGA_NKS_Flow_Users_Config v2.02`): la carpeta del shot se detectaba por PROFUNDIDAD -los segmentos 0 y 1 del prefijo-, asi que solo salia bien si el clip colgaba exactamente de `<secuencia>/<shot>/...`. Con un clip suelto bajo la secuencia o un mov de referencia, la policy se escribia sobre un prefijo que no existe, IAM lo aceptaba sin chistar y la ventana decia "asignada exitosamente" mientras el artista quedaba sin acceso. Ahora el shot se busca POR NOMBRE con `is_shot_folder_name()`, que valida el vendor code contra la DB de PipeSync -era el caso que el posicional no podia acertar, porque el naming `PROYA_010_020_VND` no matchea el patron historico-, y la secuencia pasa a ser su directorio padre. Si no se reconoce ninguna carpeta de shot y el segundo segmento es un archivo, se corta con un mensaje en vez de escribir un prefijo invalido. [ Wasabi - El shot de la policy se detecta por nombre y no por profundidad ]

        - Wasabi Policy Unassign (`wasabi_policy_utils v0.96`): sacar un shot de una policy comparaba con `in`, o sea por substring. Con el naming que lleva vendor code al final el shot base es un prefijo literal del shot con vendor, asi que quitar `PROYA_010_020` borraba tambien `PROYA_010_020_VND` y le sacaba al artista un acceso que nadie pidio sacar. Ahora se compara segmento por segmento. [ Wasabi - Sacar un shot ya no borra su variante con vendor ]

        - Wasabi Policy Assign (`LGA_NKS_Wasabi_PolicyAssign v1.00`): no se le crea policy por shot a quien tiene `skip_wasabi_policy`. Son los admins y los que acceden a proyectos completos: el permiso por shot no les agrega nada y encima queda concedido para siempre, porque el reconciliador de PipeSync filtra por ese flag y deja de administrar esa policy -pero no la borra-. El flag lo expone ahora `LGA_NKS_Flow_Users_Config`, leido de la misma tabla `flow_users`. [ Wasabi - No crear policies por shot a quien no las lleva ]

        - Show Flow Info (`LGA_NKS_Flow_Shot_info v1.96`): se saca el header interno enorme con shot | task | artistas. Ese bloque repetia lo que ya dice Task history y ocupaba media pantalla. El titulo de la ventana (donde antes decia "Info") pasa a ser `shot | task`; los artistas quedan solo en la franja. [ Flow Shot Info - Titulo solo en la barra de ventana ]

        - Show Flow Info (`LGA_NKS_Flow_Shot_info v1.95`, `LGA_NKS_TaskAssignmentHistory`, `LGA_NKS_TaskHistoryBand`): se porta la franja Task history de PipeSync. Antes el panel solo ponia un assignee en el titulo —a veces ni el activo— y no habia historial ni "Assigned then". Ahora lee `task_assignment_history` de `pipesync_stats.db` (clave = `tasks.task_id` de Flow), muestra chips colapsables + grafico, corrige el titulo con los activos, atenua los colores de autor como en notas de PipeSync y deja el amarillo de playlist sin mix. [ Flow Shot Info - Portar Task history ]

        - Los checkboxes dejan de ser los del tema del host y pasan a ser los mismos que usan las apps Qt de LGA: caja redondeada con borde, violeta con la tilde blanca cuando estan tildados. Antes cada host los dibujaba a su manera —en Nuke salian como una cruz— asi que el mismo control se veia distinto segun donde se abriera. La regla va tanto en la hoja de formulario como en la de ventana, que es la que aplican casi todas las ventanas de HieroTools. La tilde viaja como `LGA_UI_checkmark.svg` al lado del modulo de estilo —y una apagada para el estado deshabilitado— porque el QSS de Qt necesita un archivo y este pack no puede depender de los otros. [ HieroTools - Unificar los checkboxes ]

        - Los dos dialogos compartidos de `LGA_NKS_Shared` pasan al modulo de estilo. El selector de task se pintaba en `#2B2B2B`, mas claro que los botones que contiene, y su separador salia como una barra blanca porque el shadow `Sunken` de Qt le dibuja encima su propia doble linea. En el dialogo de mismatch la fila seleccionada iba en gris claro con texto negro: era el unico lugar de las tools donde una seleccion se pinta MAS clara que el fondo, y ademas su tabla era lo unico estilado mientras la ventana heredaba el tema de Hiero. Los colores de cada task no se tocan, que es lo que la ventana existe para mostrar. [ HieroTools - Unificar el estilo de los dialogos compartidos ]

        - El modulo de estilo suma `Metric.BUTTON_HEIGHT`. El boton marcado y el secundario comparten padding pero el secundario ademas lleva un borde de 1 px, asi que en una fila con los dos quedaba 2 px mas alto y 1 px mas arriba. Con el alto fijo alinean. [ HieroTools - Agregar Metric.BUTTON_HEIGHT al modulo de estilo ]

        - El modulo de estilo suma `OK_BG`, `WARNING_BG` y `ERROR_BG`, para el fondo de una pastilla de estado con texto encima. Los tres semanticos que ya estaban sirven para una barra o una linea de texto, pero a esa intensidad detras de un texto se lo comen. No cambia nada de lo que ya se ve en este pack: entra para que las cuatro copias del modulo sigan siendo el mismo archivo. [ HieroTools - Agregar los fondos de estado al modulo de estilo ]

        - El modulo de estilo deja de pisarle el campo interno a los `QSpinBox`. La hoja de formulario pinta los `QLineEdit`, y adentro de un spinbox el campo TAMBIEN es un `QLineEdit`: le caia un segundo borde y un segundo padding adentro de su propia caja, y el numero quedaba recortado contra las flechitas. El spinbox se deja nativo a proposito: en cuanto el QSS le define caja o flechas, Qt deja de dibujar los triangulos y la sub-control termina tapando el valor. [ HieroTools - Corregir el spinbox en el modulo de estilo ]

        - Nuevo `LGA_HieroTools/LGA_NKS_Shared/LGA_UI_Style_HieroTools.py`: fuente unica de la paleta, las medidas y el QSS de las ventanas. Es codigo identico al modulo de los tres ToolPacks a proposito —son repos independientes y un usuario puede tener instalado uno solo, asi que no pueden importarse entre si—, y trae ademas el coloreado de paths por nivel de directorio con la misma paleta que las apps Qt/C++ de LGA. [ HieroTools - Agregar el modulo de estilo unificado ]

        - Las tres ventanas del panel de Assignees pasan a ese modulo. No aplicaban ninguna hoja de estilo, asi que heredaban el tema de Hiero y no se parecian a las demas ventanas de las tools. Los botones dejan de ir estirados a lo ancho de la ventana —donde no se leian como botones ni se distinguia cual ejecuta la accion— y van en una fila alineada a la derecha, con el de accion ultimo y marcado en violeta. El verde puro del mensaje de exito, ilegible de tan saturado, pasa al verde de la paleta. [ HieroTools - Unificar el estilo de las ventanas de Assignees ]

        - certifi vendorizado dentro de `OIIO_Win/bin/python` (el pip 23.1.2 del Python embebido): el bundle de CAs estaba en 2022.12.07 y arrastraba GLOBALTRUST y los nueve roots de e-Tugra (CVE-2023-37920). Dependabot nunca lo reporto porque ese arbol no tiene un `requirements.txt` al lado, asi que venia publicandose en el repo y en el ZIP sin que nada avisara. Se actualiza `cacert.pem` a 2026.06.17 y la version declarada en `__init__.py`. NO se toca `core.py`: pip lleva su propia variante, distinta de la de certifi suelto, y cambiarla no aporta nada a la seguridad. Verificado con el `python.exe` embebido (3.10.7): `certifi.where()` resuelve y el bundle carga en `ssl` con 118 CAs. Ese pip no se invoca desde el codigo de las herramientas. [ HieroTools - Actualizar el certifi del Python embebido de OIIO_Win ]

        - certifi vendorizado (`shotgun_api3/lib/certifi`): el bundle de CAs estaba en 2023.07.22 y seguia confiando en GLOBALTRUST, un root que el ecosistema distrusto por misemision (CVE-2024-39689, GHSA-248v-346w-9cwc). Se actualiza a 2026.06.17, la misma que ToolPack ya tenia desde su propio arreglo y la que pinea el SDK upstream v3.10.3. `shotgun_api3` no importa certifi como modulo: arma la ruta a mano y lee unicamente `cacert.pem`, asi que lo que cambia de verdad es el bundle. Verificado que carga en `ssl` con 118 CAs y que GLOBALTRUST ya no aparece, contra 3 apariciones en el anterior. Se actualiza tambien el pin del `requirements.txt` del SDK, que es el archivo por el que GitHub avisa de los CVE. [ HieroTools - Actualizar certifi vendorizado ]

v3.86:
        - Naming de shots (`LGA_NKS_Flow_NamingUtils v1.14`, `LGA_NKS_Vendors_Config v1.00`, panel de coordinacion): el pull de Flow no encontraba ningun shot en los proyectos con vendor. El naming es `PROYECTO_SEQ_SHOT_VENDOR`, pero el parser solo conocia el formato viejo con el vendor pegado al proyecto, asi que interpretaba `VENDOR_task` como los dos bloques de descripcion: el shot code salia con la task adentro y la task salia vacia. Ahora los vendor codes se leen de la DB de PipeSync (`project_settings_cache`, clave `vendors`), que es donde los deja el Projects tab al sincronizar con Flow. No se adivinan por estructura, porque un bloque alfabetico despues de dos numeros puede ser tanto un vendor como una task (`PROJA_1048_060_Compo`). El arreglo va en el modulo central, asi que alcanza a los paneles de Flow, Coordinacion, Assignee y Edit; el mismo criterio pasa a `is_shot_folder_name()`, que reemplaza las cinco copias del regex de carpeta de shot en FileManagerS3 y PipeSync. [ Naming - Vendor code desde la DB de PipeSync ]

v3.85:
        - El `install.pdf` se reemplaza por `install_es.pdf` e `install_en.pdf`. La hoja vieja salia de exportar un Google Doc a mano y quedo congelada en el metodo manual: no menciona los instaladores que el ZIP trae desde hace varias versiones. Las dos hojas se generan ahora desde el repo de release, con la version leida de `LGA_HieroTools/VERSION`. HieroTools lleva plantilla propia porque su instalacion no se parece a la de los packs de Nuke: no toca `init.py`, instala tres items sueltos en `python/startup` y su `LGA_HieroTools_context.ini` siempre se pisa y nunca se respalda. Se documenta tambien que en macOS el instalador va con `bash installer_mac.sh`: los `.sh` pierden el permiso de ejecucion dentro del `.zip` y `./installer_mac.sh` da `Permission denied`. [ HieroTools - Reemplazar install.pdf por las hojas en castellano e ingles ]

v3.84:
        - Flow Panel (`LGA_NKS_Flow_Panel v2.58`): mismo techo de brillo que el Assignee, ahora sobre el fondo de los botones de estado, que tienen el mismo texto claro. Se corrigen `Rev Dir` (luminancia 200), `OK for Delivery` (168), `Rev Prod` (171), `Rev Charly` (150) y `Rev Sebas` (142). Se topea SOLO el `style` del botón: el `color` del mismo botón es el `QColor` que `setColor()` le aplica al clip en el timeline, y ese tiene que seguir siendo el color real del estado — topearlo habría cambiado los colores de los clips, que no es el problema que se estaba arreglando. [ Flow Panel - Techo de brillo para los botones de estado ]

        - Assignee Panel (`LGA_NKS_Flow_Assignee_Panel v1.59`, `LGA_NKS_StyleUtils v1.01`, `LGA_NKS_Projects_Panel v2.28`): los usuarios con color claro no se leían. El color va de fondo del botón y el texto es `#d8d8d8`, así que pasado cierto brillo el nombre desaparece: sobre los colores reales, los que se leen bien llegan hasta luminancia ~135 y por encima empiezan a molestar. Ahora `ensure_max_luminance()` topea el fondo en `MAX_USER_BG_LUMINANCE = 135` bajando el brillo pero manteniendo el tono; borde y hover se derivan del color ya corregido. Es la operación inversa al piso que usa el Projects Panel, donde el mismo tipo de color va como texto sobre fondo oscuro, así que las dos funciones pasan a vivir juntas en `LGA_NKS_StyleUtils` y el Projects Panel deja de tener su copia local (75 líneas menos). Solo aplica a botones de usuario con color sólido: los fijos y los gradientes no se tocan. En Flow y en PipeSync el color queda como está. [ Assignee Panel - Techo de brillo para los colores de usuario ]

        - Projects Panel (`LGA_Projects_Panel_ScanProjects v1.08`, `LGA_NKS_ProjectItem v1.02`, `LGA_NKS_ProjectHandler v1.02`): `PROJA_SUP` y `PROJA_SUP_Previa` se mostraban los dos como `PROJA`, indistinguibles en la lista. El nombre visible se armaba cortando en el primer `_SUP` con `split()`, así que todo lo que venía después se perdía. Ahora `obtener_nombre_display_proyecto()` elimina el bloque `_SUP` y conserva el resto (`PROJA_SUP_Previa` → `PROJA_Previa`), sin depender del casing y sin tocar nombres donde SUP no es un bloque propio, como `SUPERMAN`. El cálculo estaba duplicado en cuatro lugares y pasa a estar en uno solo; el color sigue saliendo del proyecto de trabajo, no del nombre visible. [ Projects Panel - Mostrar lo que va despues del SUP ]

v3.83:
        - Reglas y limpieza (`AGENTS.md` + espejos): el repo es público y tenía nombres reales de proyectos de clientes desperdigados en comentarios, docstrings, docs, fixtures de tests y changelog. Las reglas solo prohibían *agregar archivos* con rutas de clientes, no mencionarlos en texto, así que nada lo frenaba. Se agrega la sección "Nombres de proyectos y clientes" a `AGENTS.md` (sincronizada a `CLAUDE.md`, `.cursor/rules/instructions.mdc` y al depósito de release con `apply-excludes -Capturar`) y se barren 44 archivos: los nombres reales pasan a genéricos `PROJA`/`PROJB`/`PROJALT`, incluidas las variantes en carpeta `VFX-`, shot code, bucket de Wasabi y sitio de Flow. También se saca un nombre de persona real de la doc del Playlist Panel. Los dos tests con fixtures afectados siguen pasando. No se toca código funcional: los nombres reales viven en la DB de PipeSync, en Flow y en el disco. [ Reglas - Prohibir nombres de clientes y barrer los existentes ]

        - Projects Panel (`LGA_NKS_Projects_Panel v2.27`): los proyectos con color oscuro no se leían en la lista. El color sale de Flow, donde se elige como color identitario del proyecto y no como color de texto, y el panel lo usaba tal cual sobre fondo oscuro: PROJA (`#9E3A3A`, luminancia 79) y PROJB (`#966496`, 114) quedaban ilegibles. Ahora `ensure_min_luminance()` les pone un piso de 150 antes de pintarlos, en dos etapas para no lavar el color: primero sube el brillo al máximo manteniendo tono y saturación, y solo si con eso no alcanza mezcla hacia blanco lo justo. PROJA pasa a `#FF7A7A` y PROJB a `#FFAAFF`; PROJC (246) no se toca. Los grises van directo al gris del piso, porque escalarles el brillo los mandaría a blanco puro. No se modifica nada en Flow ni en la DB de PipeSync: es solo cómo se muestra. [ Projects Panel - Piso de luminancia para los colores de proyecto ]

        - Projects Panel (`LGA_Projects_Panel_SwitchSequence v2.31`): cambiar de secuencia tardaba entre 4 y 15 segundos con la UI congelada, de forma errática y sin relación con el tamaño del timeline. El switch abría la secuencia nueva y recién después destruía el viewer + timeline viejos; esa destrucción tiene que sincronizarse con los hilos de IO que están leyendo la media recién abierta y se quedaba esperándolos (13.7s medidos dentro de `sendPostedEvents`). Destruir los mismos widgets sin nada cargando en paralelo cuesta 0.46s, incluso sobre una secuencia de 129 trackItems. Ahora el cierre va ANTES de la apertura, detrás del flag `CLOSE_BEFORE_OPEN` para poder volver atrás. El playhead se captura y se restaura a mano, porque `openInTimeline` lo preservaba leyéndolo del viewer previo, que con el orden nuevo ya no existe. [ Projects Panel - Cerrar el timeline viejo antes de abrir el nuevo ]

v3.82:
        - FileManagerS3 (`LGA_NKS_FileManagerS3Launcher`, panel de coordinación, `fm_cli_mac.sh`): el repo de la app pasó a llamarse `LGA_FileManagerS3` y renombró sus símbolos, así que las tres rutas al build de desarrollo (`MAC_DEV_APP`, `DEFAULT_DEV_APP` y el ejemplo de la doc) apuntaban a una carpeta inexistente. Se corrigen y se barre el nombre viejo en 18 archivos: los cinco módulos `LGA_NKS_FileManager_*` pasan a `LGA_NKS_FileManagerS3_*` (con las cadenas de `importlib` del panel, que los carga por nombre de archivo), más `get_filemanagers3_exe`, `build_filemanagers3_command` y el test. Se preservan a propósito las aserciones de `_assert_no_legacy_targets`, que verifican que NO se usen `FileManager.exe` ni `FileManager.app`: renombrarlas habría invertido su sentido. El test pasa antes y después.

        - El pack declara su version instalada en `LGA_HieroTools/VERSION`, dentro del pack y no suelto en `python/startup`, que es una carpeta compartida. El instalador lo exige como archivo obligatorio del payload y el generador lo publica junto al README. Queda como base para comparar contra la version disponible en GitHub. [ HieroTools - Agregar VERSION al pack ]

v3.81:
        - Projects Panel (`LGA_NKS_CheckProjectVersions v1.83`, `LGA_Projects_Panel_ScanProjects v1.07`): con dos proyectos en la misma carpeta, abrir `PROJB_v001` mostraba la flecha de update ofreciendo `PROJB_BREAKDOWN_v05` como versión nueva de PROJB. `encontrar_version_mas_alta()` buscaba las versiones con un glob por prefijo (`PROJB*v*.hrox`), y ese comodín también matchea los archivos de otro proyecto cuyo nombre empieza igual. Ahora lista los `.hrox` de la carpeta y los filtra por clave de proyecto, la misma que usa el escaneo para agrupar — el escaneo pasa a delegar en esta función para que no haya dos criterios que se desincronicen. Arregla también la ventana de Check Project Versions, que tenía el mismo problema. [ Projects Panel - La version mas alta ya no sale de otro proyecto ]

        - Projects Panel (`LGA_Projects_Panel_ScanProjects v1.06`, `LGA_NKS_Workers v1.01`): al pasar de studio a client (o al revés) los proyectos abiertos del contexto anterior seguían apareciendo como abiertos en la pestaña nueva. El escaneo de disco sí cambiaba de root, pero la lista de abiertos salía de `hiero.core.projects()` sin filtrar, así que mostraba proyectos que no existen en el disco de esa pestaña. Ahora `get_open_projects_info()` recibe el root del contexto y descarta los `.hrox` que no cuelgan de él; el mismo filtro aplica al chequeo de versiones nuevas, cuyo `base_path` default era `T:\` hardcodeado. La comparación es por componentes de path para que `T:\VFX-ABC` no cuente como hijo de `T:\VFX-AB`. [ Projects Panel - Filtrar proyectos abiertos por contexto ]

        - Projects Panel (`LGA_NKS_Project_Colors_Config v1.00`, `LGA_NKS_Projects_Panel v2.26`): los colores de proyecto salen de PipeSync y no del `.ini` local. La fuente de verdad es Flow (`Project.sg_pipesync_project_settings_json`, campo `project_color`), que PipeSync cachea en la tabla `project_settings_cache` de `pipesync_stats.db`; los nombres salen de la tabla `projects`. Se lee read-only la DB del contexto activo, así que studio y client toman cada uno los colores de su sitio. Antes cada máquina tenía su propia sección `[Colors]` y el mismo proyecto se veía de un color distinto por persona. No hay fallback: sin sync, el proyecto usa el `#cccccc` por defecto. Se elimina `[Colors]` del `.ini`, que queda solo con `AutoRefreshInterval`, y el editor de colores del panel de settings pasa a ser una lista read-only con el motivo a la vista cuando no hay datos. [ Projects Panel - Colores de proyecto desde la DB de PipeSync ]

        - Projects Panel (`LGA_Projects_Panel_ScanProjects v1.05`, `LGA_NKS_ProjectHandler v1.01`, `LGA_NKS_ProjectItem v1.01`): con dos proyectos en la misma carpeta, `PROJB_Breakdown` salía en gris en vez del rosa de PROJB. El color se buscaba en el `.ini` usando el nombre del archivo recortado en `_SUP`, que para `PROJB_SUP` daba `PROJB` pero para `PROJB_Breakdown` no tenía nada que recortar y no matcheaba ninguna clave. Ahora el proyecto de trabajo sale de la carpeta `VFX-<proyecto>` de la ruta y viaja como `project_key` en cada item, así que todos los `.hrox` de un mismo proyecto comparten color. Los proyectos abiertos lo derivan de su ruta en disco en vez de fabricar el `vfx_folder` desde el nombre base. [ Projects Panel - Color por carpeta VFX y no por nombre de archivo ]

        - Projects Panel (`LGA_Projects_Panel_ScanProjects v1.04`): el escaneo listaba un solo proyecto por carpeta `*_SUP`, así que en `T:\VFX-PROJB\PROJB_SUP` aparecía `PROJB_SUP` y quedaba invisible `PROJB_Breakdown`. La causa era que se tomaba el primer `.hrox` de la carpeta como referencia y se buscaba su versión más alta, ignorando los demás nombres base. Ahora los `.hrox` se agrupan por proyecto y se elige la versión más alta de cada grupo, entrando cada uno como item propio con sus secuencias. La clave de agrupación descarta versión y sufijo, de modo que `PROJB_SUP_v040` y `PROJB_SUP_v41_Mac` siguen siendo el mismo proyecto. [ Projects Panel - Listar todos los proyectos de cada carpeta SUP ]

        - Docs (`LGA_NKS_FileManager.md`): la sección Referencias Técnicas seguía listando `get_filemanager_exe()` como función de `LGA_NKS_FileManager_DownloadClip.py`. Esa función no existe: la borró el commit que centralizó el launcher, que movió la resolución del ejecutable a `LGA_NKS_Shared/LGA_NKS_FileManagerLauncher.build_filemanager_command()`. El párrafo de más arriba ya se había corregido en la pasada de mojibake, pero la línea de Referencias Técnicas quedó afuera. Ahora nombra solo `build_filemanager_cmd()`, que sí existe, y apunta al launcher para la resolución del exe. Se barrió el resto de la sección: los otros nombres que no aparecen como `def` son métodos de la API de Hiero o funciones de `main.cpp` del repo de FileManager, y están bien. [ Docs - Corregir get_filemanager_exe en las referencias técnicas ]

        - Docs (`LGA_NKS_FileManager.md`, `+Building_Blocks/fix_mojibake.py`): la doc del FileManager estaba con mojibake completo (UTF-8 leido como cp1252 y re-encodeado tres veces: "aplicaciÃƒÆ’Ã‚Â³n"), asi que era ilegible y no se podia editar por texto. Se arreglo con una tool y no a mano: busca rachas de caracteres sospechosos y les aplica el viaje de vuelta cp1252/UTF-8 en loop, dejando intacto el texto correcto porque un acento legitimo no es UTF-8 valido al encodearlo. 172 reemplazos, cero restos, y el resto del repo dio limpio. Hubo que sumar las 5 posiciones que cp1252 deja sin definir (0x81, 0x8D, 0x8F, 0x90, 0x9D), donde quedaban trabados los emojis. Aprovechando que quedo legible se corrigieron tres datos vencidos: el doble click de Download Clip (v3.80 lo invirtio), la ruta del exe instalado y `get_filemanager_exe()`, que ya no existe. [ Docs - Arreglar mojibake de la doc de FileManager ]

v3.80:
        - FileManager Launcher (`LGA_NKS_FileManagerLauncher`): la ruta de la app instalada cambia de `C:\Portable\LGA\FileManager\FileManagerS3.exe` a `C:\Portable\LGA\FileManagerS3\FileManagerS3.exe`. FileManager S3 v0.906 renombra su carpeta de instalacion para liberar el nombre `FileManager`, que queda reservado para la app local que viene despues. `WINDOWS_PROD_EXE` es una constante hardcodeada y sin este cambio los `--path` / `--download` dejan de encontrar el exe. **Los dos updates hay que hacerlos juntos**: con solo uno de los dos actualizado el launcher falla. La ruta de DEV no se toca, porque apunta al build del repo de FileManager, que conserva su nombre. Tampoco cambia como se elige el contexto: se sigue llamando al MISMO exe pasando `--context studio|client` segun `LGA_HieroTools_context.ini`, y no existe un ejecutable client aparte. [ FileManager Launcher - Apuntar a la carpeta FileManagerS3 ]

        - Docs (`LGA_NKS_Flow_Pull v3.57`, `LGA_NKS_Wasabi_PolicyUnassign_CompletedShots v1.02`, `Docu_Flow_Estados_Colores.md`): barrido de referencias a los nombres viejos de la cola de entrega en los dos repos. En HieroTools quedaban textos de UI con los nombres anteriores: el titulo y los dos botones de seleccion de la ventana de limpieza de policies de Wasabi, que decian "Approved" y "Delivery Ok". Se documenta ademas por que `hiero_status_dict` del Pull conserva el color de `Rev Dir Den` aunque el boton ya no exista: los clips pintados con ese violeta siguen en timelines viejos y sin la entrada el Pull los mostraria como "Unknown". En PipeSync se actualizaron el tooltip del sync de Wasabi, los comentarios de `StatusContextPolicy`, `ApprovedCleanupUtils`, `ApprovedCleanupManager`, `ApprovedSoftCleanupManager` y `ShotCard`, y los docs `Doc_Status_Sync` (con la cola y las dos trampas del schema de Flow), `Doc_Wasabi_Policies`, `Doc_Tab_Stats`, `Doc_Tab_Project_Settings`, `Doc_Tab_Tools_Cleanup` y `Plan_Colapsar_Tasks_Aprobadas`. [ Docs - Barrer los nombres viejos de la cola de entrega ]

        - Flow / Flow Panel / Create Shot (`LGA_NKS_Flow_Panel v2.57`, `LGA_NKS_Flow_CreateShot v1.46`, `LGA_NKS_Flow_Status_Config`, `LGA_NKS_Wasabi_PolicyUnassign_CompletedShots v1.01`): la cola de entrega estaba mal en Flow y el estado final se llamaba casi igual que el primero. Mirando 1710 shots de wanka quedo claro cual es el orden real: 14 proyectos terminados estan 100% en `apr` y ninguno en `check`, y en los proyectos mezclados `apr` siempre se toco despues — o sea `pubsh` -> `check` -> `apr`, con `apr` como FINAL. Los dos sitios de Flow lo tenian al reves (`apr` en el medio) y wanka ademas tenia `revjav` antes que `revjua`. Se reordenan los cuatro `sg_status_list`. En el Shot de projbvfx se reemplaza `pbshed` por `check`: la app manda `check` y projb no lo aceptaba, asi que marcar un shot como entregado desde Client fallaba en silencio; `pbshed` no lo usaba ningun shot. Aparte, el estado final se mostraba como "Delivery OK" y el primero como "OK for Delivery" — las mismas tres palabras en los dos extremos opuestos; pasa a **"Delivery Apr"**. Los display names de los dos sitios quedan alineados con la app (`check` -> "Delivered", `omit` -> "Omited", `rev_su` -> "Review Sebas", `revjav` -> "Review Javi" que estaba escrito `review_javi`, `revleg` -> "Review Lega", y la capitalizacion de `noread`/`ready`/`progre`/`plylst`); en projbvfx `revleg` queda "Review Sup" a proposito. Ningun `code` de Flow cambia y no se toca ni un dato de shots o tasks. Se elimina el boton `Rev Dir Den`, que empujaba el mismo `rev_di` que `Rev Dir` y solo cambiaba el color del clip. Nota para la proxima: `schema_field_update` sobre `valid_values` revienta con "undefined method 'empty?' for nil" si el campo no tiene `default_value`; projbvfx lo tenia vacio y hubo que setearlo en `noread` (como ya estaba wanka) antes de poder reordenar. [ Flow Status - Corregir el orden de la cola de entrega y renombrar apr ]

        - Flow Panel (`LGA_NKS_Flow_Panel v2.57`): el boton Rev Dir nunca limpio los tags del clip. El chequeo comparaba contra `"Rev_Dir"` con guion bajo y el boton se llama `"Rev Dir"` con espacio, asi que no matcheaba nunca; con Corrections si funcionaba porque ese string coincidia. Los dos nombres pasan a una constante `CLEAR_TAG_BUTTONS`. [ Flow Panel - Rev Dir vuelve a limpiar tags ]

        - Flow Panel / Create Shot (`LGA_NKS_Flow_Panel v2.56`, `LGA_NKS_Flow_CreateShot v1.45`): se elimina el boton `Rev Dir Den` en los dos contextos; empujaba el mismo `rev_di` que `Rev Dir` y solo se diferenciaba por el color del clip. Los estados pasan a llamarse y a ordenarse igual que en Flow: el orden sale del `sg_status_list` de cada sitio — ojo que en Flow `revjav` va ANTES que `revjua`, al reves de como estaban los botones — y `check` pasa de "Delivered" a "Delivery Checked", que es su nombre real. Quedan cuatro divergencias de nombre declaradas y documentadas, todas porque el nombre de Flow es interno o esta mal escrito: `rev_su` (Flow "Review Sup", mostramos "Review Sebas"), `revjav` (Flow "review_javi"), `revleg` (Flow "RevLega" en wanka y "Review Sup" en projb) y `omit` (Flow "omitted"). El test de verificacion pasa a comparar tambien ORDEN y NOMBRES contra el schema real de los dos sitios, no solo el conjunto de codigos. `revprd` toma el mismo tag de XYplorer que `rev_di`. [ Flow Status - Sacar Rev Dir Den y alinear nombres y orden con Flow ]

        - Flow Panel / Create Shot / Pull / Push (`LGA_NKS_Flow_Status_Config v1.00`, `LGA_NKS_Flow_Panel v2.55`, `LGA_NKS_Flow_Pull v3.56`, `LGA_NKS_Flow_Push v4.06`, `LGA_NKS_Flow_Push_connector v1.05`, `LGA_NKS_Flow_CreateShot v1.44`): los estados de Flow pasan a una fuente unica, `LGA_NKS_Shared/LGA_NKS_Flow_Status_Config.py`, y se filtran por contexto. Los dos sitios de Flow NO tienen la misma lista de `sg_status_list`: `rev_su`/`revcha`/`revjua`/`revjav` existen solo en wanka y `revprd` (Review Prod) solo en projbvfx, asi que el panel ofrecia en Client cuatro botones que Flow rechazaba con "'xxx' is not a valid status" y no ofrecia Review Prod en ningun lado. El mismo dato vivia copiado en cinco lugares (`status_translation` en Push y en el conector, `task_status_dict` en Push y en Pull, mas la lista de botones del panel) y las copias ya estaban desincronizadas entre si. Se suma `pubsh` (OK for Delivery) en los dos contextos, se renombran los visibles a como se llaman en Flow (`Approved` -> `Delivery OK`, `Delivery Ok` -> `Delivered`) y se alinean los colores de clip de la cola verde (`rev_di` `#B5DB4B`, `apr` `#266612`, `check` `#38A138`, `revhld` `#9E6A15`); `revprd` usa `#8CBF3F` y no el `#D7F2B1` de Flow, que tiene mas luminancia que el gris de `noread` y en un clip chico se lee como blanco. El catalogo de estados NO se filtra por contexto a proposito: solo se filtra lo que ESCRIBE estado (botones y dropdowns), porque la DB local puede tener codigos sincronizados del otro sitio y filtrarlos los haria desaparecer de la tabla del Pull sin aviso. Create Shot suma ademas `pbshed` (Delivered), que es el "entregado" de shot en projb donde `check` no existe. [ Flow Status - Fuente unica de estados y filtrado por contexto ]

        - Assignee Panel (`LGA_NKS_Flow_Assignee_Panel v1.58`, `LGA_NKS_Wasabi_PolicyUnassign_CompletedShots v1.01`): en contexto Client el panel queda deshabilitado con el motivo a la vista, en vez de dibujarse con los dos botones fijos y ningun usuario — que se leia igual que "PipeSync todavia no sincronizo", un problema distinto y con arreglo. En Client no hay assignees por tres razones acumuladas: el envelope `HumanUser.sg_pipesync_user_json` es un custom field que solo existe en el sitio de studio y todos los usuarios llegan con `assignable = 0`; la `pipesync_stats.db` de client no tiene las columnas `panel_order`/`skip_wasabi_policy`, asi que la query de `load_flow_users()` levantaba `OperationalError`; y las policies de Wasabi por shot no aplican porque ahi el acceso va por Vendor Groups. El panel tampoco abre ya la DB en Client. Aparte, la ventana de limpieza de policies reconoce `pubsh` y `pbshed` como shot terminado: sin ellos salia vacia en Client, donde el entregado es `pbshed` y no `check`. [ Assignee Panel - Deshabilitar en modo Client ]

        - Paneles / contexto (`LGA_NKS_ContextSwitch v1.00`, `LGA_NKS_Projects_Panel v2.25`, `LGA_ViewerPanel v1.72`): el switch Studio/Client avisa a los demas paneles por un bus, que se reconstruyen sin reiniciar Hiero. Toda esa maquinaria esta detras de un gate: el switch existe para un solo usuario y el resto tiene contexto FIJO definido por el zip que instalo, asi que si `has_context_switch()` da False, `subscribe()` y `notify()` no hacen nada — no se instancia el QObject del bus, no se conecta ninguna senal y no queda ningun callback vivo. Esos paneles leen el contexto una vez en `__init__`, que es una lectura de INI que ya ocurria. El gate ademas memoiza el `Flow.Login` del perfil normal, que implica leer y desencriptar `config.secure` y hasta ahora se resolvia cuatro veces por arranque (Projects Panel, su UIManager, ViewerTL y el propio gate): queda en una. El aviso se emite DESPUES de escribir el INI, porque los suscriptos releen el contexto y tienen que ver el valor nuevo. [ Paneles - Bus de contexto con gate por usuario ]

        - Flow (config del sitio, sin cambio de codigo): se habilito `pubsh` (OK for Delivery) en el `sg_status_list` de Shot y Task del sitio wanka, insertado antes de `apr` igual que en projbvfx. La entidad `Status` ya existia en los dos sitios, pero que exista no alcanza: lo que Flow valida al escribir es la lista de valores del campo, y ahi no estaba. Shot pasa de 7 a 8 codigos y Task de 14 a 15. [ Flow - Habilitar OK for Delivery en el sitio de studio ]

        - Assignee Panel (`LGA_NKS_Flow_Users_Config v2.01`): el orden de los botones sale de `panel_order`, un campo del envelope de cada usuario en Flow: los que tienen numero van primero por ese numero, el resto alfabetico. Es el MISMO orden que usa el popover de assignees de PipeSync — un solo dato, no dos listas para mantener sincronizadas a mano. Se edita desde el campo `List order` del Projects tab de PipeSync. Se resuelve en el SQL con un CASE porque 0 significa "sin orden" y ordenar por el numero crudo pondria a esos primeros. [ Assignee Panel - Orden de botones desde panel_order ]

        - Docs (`LGA_NKS_Flow_Assignee_Panel_README.md`): reescrito para el modelo nuevo. El README seguia explicando como agregar, modificar y eliminar usuarios editando `LGA_NKS_Flow_Users.json` — instrucciones que ya no funcionan y que mandaban a tocar un archivo que dejo de leerse. Ahora explica de donde salen los usuarios (Flow -> `pipesync_stats.db` -> panel), que los cambios se hacen desde el Projects tab de PipeSync, que hace falta un sync para verlos, que sin PipeSync corrido el panel se queda sin botones y que no hay fallback. Se agrega una seccion de referencias tecnicas con enlaces cruzados al repo de PipeSync (`Doc_Assignee_User_Colors.md`, `fetch_flow_users()`, el bootstrap del custom field y el dialogo de edicion). [ Docs - Reescribir el README del Assignee Panel ]

        - Assignee Panel / Wasabi / Flow Panel (`LGA_NKS_Flow_Users_Config v2.00`, `LGA_NKS_Flow_Assignee_Panel v1.57`, `LGA_NKS_Flow_Assign_Assignee v1.26`, `LGA_NKS_Flow_Assignee v1.26`, `LGA_NKS_Flow_Clear_Assignees v1.26`, `LGA_NKS_Wasabi_PolicyAssign v0.99`, `LGA_NKS_Wasabi_PolicyUnassign v0.61`, `LGA_NKS_Flow_Shot_info v1.94`): los usuarios (nombre, color y usuario de Wasabi) ahora salen de la DB de PipeSync (`pipesync_stats.db`, tabla `flow_users`), que PipeSync baja de Flow en su sync. La fuente de verdad pasa a ser un envelope por persona en `HumanUser.sg_pipesync_user_json`, editable desde el Projects tab de PipeSync. Se elimina `LGA_NKS_Flow_Users.json` como fuente: estaba desincronizado de PipeSync sin que nadie se enterara y la misma persona se veia de un color en cada app. Tambien se van la config por defecto del panel y, en `Shot_info`, el mapa de colores hardcodeado y el caso especial de Nombre Apellido. `LGA_NKS_Flow_Users_Config` pasa a v2.00 con una API nueva (`load_flow_users`, `find_user_by_name`, `find_user_by_wasabi_user`) que lee la DB en modo read-only. NO hay fallback: sin datos el panel se queda sin botones de usuario y lo loguea, en vez de mostrar una lista compilada que puede estar años vieja. [ Assignee Panel - Los usuarios y colores salen de la DB de PipeSync ]

        - Coordination Panel (`LGA_NKS_Flow_FlowProd_Panel v1.25`): invertido el comportamiento del boton Download Clip: click ahora descarga la ultima version del clip, Shift+Click descarga la version seleccionada. Tooltip actualizado acorde. [ Download Clip - Invertir click y shift+click ]

        - Edit Panel / Review Panel (`LGA_EditToolsPanel v3.02`, `LGA_ReviewPanel v2.82`, `LGA_NKS_SelfReplaceClip v1.21`): movido el boton Self ReplaceClip del Review Panel al Edit Panel, ubicado debajo de Reconnect Media con el mismo color (#4a4329). El script sigue en LGA_NKS_Shared. [ Self ReplaceClip - Mover de Review a Edit Panel ]

        - Playlist Panel (`LGA_NKS_FlowPlaylist_Push v0.05`, `LGA_NKS_FlowPlaylist_Push_connector v0.05`): se marca el Playlist Panel como abandonado. Se agrega `LGA_NKS_Playlist_Panel_py/_ABANDONADO_NO_TRABAJAR_ACA.md` y una seccion en `AGENTS.md` / `CLAUDE.md` / `.cursor/rules/build.mdc` para que ningun agente trabaje ahi por iniciativa propia ni lo incluya en barridos generales del repo. De paso se corrige la ruta del conector, que apuntaba a `LGA_NKS_Flow_Push_connector.py` dentro de la carpeta del Playlist (donde no existe) y hacia que el push siempre cortara con "Conector no encontrado", y el mensaje de error del conector que se identificaba con el nombre del conector del Flow Panel. [ Playlist Panel - Marcar abandonado y corregir ruta del conector ]

        - Flow Push / Playlist Push (`LGA_NKS_Flow_Push v4.05`, `LGA_NKS_Flow_Push_connector v1.04`, `LGA_NKS_FlowPlaylist_Push v0.04`, `LGA_NKS_FlowPlaylist_Push_connector v0.04`): limpieza de codigo muerto en la cadena de Push (~1060 lineas). Se eliminan las clases `ShotGridManager` de los dos paneles (nunca se instanciaban: el push siempre va por el conector via subprocess), las operaciones inalcanzables del dispatcher de los conectores (`find_shot_and_tasks`, `find_highest_version`, `update_task`, `update_version`, `get_task_assignee`, `add_comment`, `attach_images`, `check_version`; quedan `list_versions_for_task` y `execute_full_push`), el segundo bloque `except ImportError` con el fallback de naming que era inalcanzable porque el primer handler ya captura, `MultipleShotsDialog`, `_show_task_selection_dialog` (solo en Flow Push; Playlist si lo usa), `DBManager.find_project`, `get_project_id_from_version`, el wrapper legacy `get_task_assignee` y los imports sin uso `tempfile`/`shotgun_api3`/`Path`. El import de naming de los conectores ahora falla explicito con `raise` en vez de continuar hasta un `NameError` posterior. Sin cambios de comportamiento en el push. [ Flow Push - Limpiar codigo muerto de la cadena de Push ]

        - Flow Push / Playlist Push (`LGA_NKS_Flow_Push v4.04`, `LGA_NKS_Flow_Push_connector v1.03`, `LGA_NKS_FlowPlaylist_Push v0.03`, `LGA_NKS_FlowPlaylist_Push_connector v0.03`): la DB local se trata explicitamente como cache de Flow y nunca se escribe antes ni sin confirmacion de Flow. Los conectores dejan de tragarse los errores de `update_task_status` / `update_version_status` (ahora devuelven `(ok, error)`), el push aborta con error si falla el estado de la Task y `execute_full_push` devuelve un dict `applied` con lo realmente escrito. Los workers actualizan la DB solo con esas partes confirmadas: estado de version replicado tal cual lo aplico Flow y nota local solo si Flow creo la nota. Se elimina la traduccion divergente que escribia `unvleg` en la DB para `revleg` cuando Flow recibia `vwd`, y una Version inexistente en Flow ya no queda como estado nuevo en la DB. [ Flow Push - Escribir DB solo tras confirmar Flow ]

        - Coordination Panel / FileManager (`LGA_NKS_FileManagerLauncher v1.01`, `fm_cli_mac.sh`): en macOS se unifica la fuente de verdad para resolver `FileManagerS3.app`; el helper resuelve la ruta dev/prod según el flag `Desarrollo` con `_resolve_macos_app` y se la pasa al wrapper vía `--app-path`, que queda solo como capa de exec (`open -na`). Antes el wrapper preferia incondicionalmente la app dev e ignoraba `Desarrollo`. `FILEMANAGER_APP_PATH` sigue teniendo prioridad y el uso manual del wrapper conserva su fallback dev/prod; se amplían casos macOS dev/prod en `test_filemanager_launcher.py`. [ FileManager Launcher - Unificar resolucion macOS con flag Desarrollo ]

        - Coordination Panel / FileManager (`LGA_NKS_FileManager_DownloadClip`, `LGA_NKS_FileManagerLauncher`): DownloadClip importa el launcher dentro del guard `if utils_path.exists()` para degradar con gracia en Hiero como las otras tres tools, en vez de romper la carga del módulo si falla el import. El launcher deja de filtrar args vacíos silenciosamente (que dejaban flags colgados como `["--path"]`) y ahora rechaza con `ValueError` claro el valor vacío de un flag; se amplían los casos en `test_filemanager_launcher.py`. [ FileManager Launcher - Guard de import y rechazo de args vacíos ]

        - Coordination Panel / FileManager (`LGA_NKS_FileManagerLauncher v1.00`, OpenPath/Download/Upload/DownloadClip v1.01): se centraliza el lanzamiento en `LGA_NKS_Shared/LGA_NKS_FileManagerLauncher.py` para usar siempre `FileManagerS3` en dev/prod y Windows/macOS, con `--context studio|client` explícito según `LGA_NKS_ContextProfile`, sin `shell=True` y con errores claros cuando faltan binarios. Las cuatro tools migran al helper único conservando flags CLI y `--notify-completion`; se elimina el uso runtime de rutas legacy `FileManager.exe`/`FileManager.app`. Se agrega prueba unitaria `test_filemanager_launcher.py` para validar contexto, rutas y exclusión de targets legacy. [ FileManager Launcher - Centralizar FileManagerS3 con contexto explícito ]

        - Assignee Panel / Wasabi tests (`test_bucket_resolver.py`): se robustece el writer concurrente de la prueba de lectura estable con retry de `os.replace` en Windows y cierre garantizado de thread para evitar falsos bloqueos por `PermissionError` transitorio durante reemplazo atómico. [ Wasabi Policy - Estabilizar test concurrente en Windows ]

        - Assignee Panel / Wasabi (`LGA_NKS_BucketResolver v1.02`, `SecureConfig_Reader v1.02`): el snapshot runtime queda cacheado por ruta canónica + perfil/contexto para impedir reuse cruzado Studio↔Client en fallos de lectura; `ProjectBucketOverrides` valida project keys como segmento seguro (`[A-Z0-9_-]`, sin separadores ni `.`/`..`) y mantiene fail-closed con diagnóstico accionable. `SecureConfig_Reader` reemplaza el byte-lock local por lectura estable con fingerprint+retry compatible con reemplazo atómico de `config.secure`, evitando payload parcial durante escrituras concurrentes. [ Wasabi Policy - Aislar cache por contexto y lectura estable ]

        - Assignee Panel / Wasabi: el resolver conserva y reporta las claves de proyecto que quedan vacias despues de normalizar, en vez de ignorarlas como si el mapping fuera valido. [ Wasabi Policy - Corregir diagnostico de clave de proyecto vacia ]

        - Assignee Panel / Wasabi (`LGA_NKS_Wasabi_PolicyAssign v0.98`, `LGA_NKS_BucketResolver v1.01`, `SecureConfig_Reader v1.01`): el mapping proyecto<->bucket adopta contrato fail-closed (schema no-object, non-string, duplicados/colisiones y ambigüedad reverse bloquean la resolución), se preserva último snapshot válido ante fallos transitorios de lectura y `parse_path_for_policy` propaga errores accionables por ruta en lugar de continuar silenciosamente. `SecureConfig_Reader` agrega lectura con shared lock y API `read_secure_config_with_status()` para evitar lecturas parciales de `config.secure`. [ Wasabi Policy - Fail-closed y lectura bloqueada de config ]

v3.79:
        - Assignee Panel / Wasabi (`LGA_NKS_Wasabi_PolicyAssign v0.97`, `LGA_NKS_BucketResolver v1.00`): se agrega un resolver central de bucket por contexto (Studio/Client) leyendo `ProjectBucketOverrides` desde `config.secure` activo. La generación de policies deja de derivar bucket con `parts[1].lower()` y usa el mapping lógico->físico sin cambiar `VFX-*`, proyecto Flow ni shot codes. Se documenta el contrato en `docs/Doc_ProjectBucketResolver.md`. [ Wasabi Policy - Resolver bucket contextual para Assignee ]

        - Packaging (`_LGA_ReleaseGen-HieroTools.bat`, `i_win_engine.ps1`): el zip publico/gh siempre viaja con `LGA_HieroTools_context.ini` en `mode = client` y el zip interno de estudio siempre en `mode = studio`. Los INI fuente son `LGA_HieroTools_context_gh.ini` y `LGA_HieroTools_context_studio.ini`, ambos versionados en `Python/Startup/`, verificados por preflight (existencia + valor de `mode`) y stageados a un temp dir para agregarse a cada zip como `Startup/LGA_HieroTools_context.ini`. El installer copia ese INI a `python/startup/` pisando siempre el previo, para que la editora del cliente arranque en client sin tocar nada y los reviewers de estudio siempre en studio. Documentado en `docs/Doc_HieroTools_Studio_Client_Context.md`. [ Packaging - Fijar context.ini por zip e instalar pisando ]

        - ViewerTL (`LGA_ViewerPanel v1.71`): los botones dinamicos Prev/Next Rev y sus shortcuts `Ctrl+Alt+Shift+,` / `Ctrl+Alt+Shift+.` ahora aparecen tambien cuando Hiero se abre en modo client. La identidad del reviewer local se lee siempre desde el perfil PipeSync normal (`get_normal_pipesync_flow_login`), no desde el contexto activo, porque quien usa las tools es una propiedad de la maquina/instalacion y no del proyecto abierto. Antes, en modo client se resolvia al `Flow.Login` del `PipeSyncClient/config.secure` (editora), no matcheaba ningun usuario en `usuarios_config` y los botones no se creaban. [ ViewerTL - Botones Prev/Next Rev en modo client ]

        - ViewerTL (`LGA_ViewerPanel v1.70`, `LGA_NKS_SnapShot v0.62`): Shift+Click en SnapShot abre la captura recortada en ShareX ImageEditor LGA sin generar JPG ni cache. El click normal conserva la copia al portapapeles. [ ViewerTL - Abrir Snapshot en editor con Shift ]

        - Review Panel (`LGA_ReviewPanel v2.81`): suma al final los botones Next Annotation y Previous Annotation. Navegan las anotaciones del clip seleccionado en ambas direcciones y vuelven al extremo opuesto al superar la primera o ultima anotacion. [ Review Panel - Navegar anotaciones del clip ]

        - Projects Panel (`LGA_NKS_Projects_Panel_SwitchSequence v2.29`): el log del cambio de timeline ahora registra snapshots de viewers/timelines, widgets agendados con `deleteLater()`, tiempos de `processEvents` / `DeferredDelete` y una espera final post-event-loop para detectar en que punto real Hiero queda ocupado cerrando o rearmando timelines. [ Projects Panel - Mejorar diagnostico de cambio de timeline ]

v3.78:
        - Flow Push (`LGA_NKS_Flow_Push v4.03`, `LGA_NKS_Flow_Push_connector v1.02`): si Flow encuentra proyecto/shot/task pero no encuentra la Version, el Push ya no termina directamente en error. Ahora pregunta si se quiere cambiar igualmente el estado de la Task sin enviar mensaje ni adjuntar imagenes; al aceptar, actualiza la Task, pinta el clip y actualiza la ventana Pull abierta si corresponde. [ Flow Push - Permitir task sin version ]

        - Flow Push (`LGA_NKS_Flow_Push v4.02`): los errores del worker/conector ahora se muestran con `QMessageBox` en la UI, incluyendo proyecto, shot, task, version, estado solicitado y archivo. Esto cubre casos como Push fallido porque Flow no tiene version para el shot/task, que antes quedaban solo en el log y parecian un cuelgue sin feedback. [ Flow Push - Mostrar errores en UI ]

        - Flow Push / Pull (`LGA_NKS_Flow_Panel v2.54`, `LGA_NKS_Flow_Pull v3.55`): cuando un Push exitoso cambia el color de un clip, si hay una ventana de Pull abierta con ese shot/task en la tabla, la fila se actualiza en vivo: el `New Status` anterior pasa a `Previous Status` y `New Status` muestra el estado recien pusheado. [ Flow Push - Actualizar ventana Pull abierta ]

        - Task / Track Mismatch (`LGA_NKS_TaskMismatchDialog v1.02`, Flow Pull v3.54): la ventana sigue siendo no modal, pero ahora la ventana siguiente del Pull queda diferida hasta que el usuario cierre la advertencia. El mensaje superior aclara que se puede clickear una fila para ubicar el clip en timeline, y la seleccion de filas pasa a gris/blanco en lugar del amarillo default. [ Task Mismatch - Diferir Pull y ajustar seleccion ]

        - Task / Track Mismatch (`LGA_NKS_TaskMismatchDialog v1.01`): la advertencia de clips en tracks incorrectos deja de ser modal/bloqueante, suma checkbox persistente `Keep this window on top` abajo a la izquierda (on por defecto) y permite clickear una fila para seleccionar el clip, setear In/Out desde el propio clip y mover el playhead. [ Task Mismatch - Ventana no modal con navegacion ]

        - Flow Pull (LGA_NKS_Flow_Pull v3.53): la tabla de resultados muestra el estado `rev_su` como `Review Sebas` en lugar de `Review Sup`, manteniendo el codigo Flow y el tag interno `Rev_Sup`. [ Flow Pull - Mostrar Review Sebas ]

        - Flow Pull (LGA_NKS_Flow_Pull v3.52): normaliza las comparaciones de colores hex a minusculas para que `#7f4b69` y `#7F4B69` sean el mismo estado. Esto evita que Pull muestre `Unknown -> Review Juano` y detecte un falso cambio cuando el Push ya habia pintado correctamente el clip con Rev Juano. [ Flow Pull - Normalizar colores hex ]

        - Flow Pull (LGA_NKS_Flow_Pull v3.51) mueve `Keep this window on top` al header, en la misma linea que `ProjectName / SeqNumber` y alineado a la derecha. Flow Panel (v2.53) corrige el `QColor` de `Rev Juano` para que el Push pinte clips con el mismo `#7F4B69` que Pull; ViewerTL Prev/Next Rev (v1.27) tambien busca ese color real para Juano. [ Flow Pull y Push - Header keep on top y color Rev Juano ]

        - ViewerTL suma botones dinamicos Prev/Next Rev para Charly y `LGA_NKS_PrevNext_Rev` reconoce el color `#a9909d` de `revcha`. Se agrega `docs/Docu_Flow_Reviewer_Onboarding.md` con el checklist de archivos y puntos a modificar cada vez que entra o sale un reviewer del estudio, y se corrige la doc de estados para incluir a Charly en la descripcion de checkboxes. [ ViewerTL - Agregar Rev Charly y doc reviewers ]

        - Flow Pull (LGA_NKS_Flow_Pull v3.50): la ventana de resultados ahora tambien muestra las tasks que ya estaban en review para el usuario actual, aunque no hayan cambiado ni tengan mismatch de version. El mapeo sigue ViewerTL: Lega `revleg`, Sebas `rev_su`, Juano `revjua`, Javi `revjav`, y se suma Charly `revcha`/`review_charly`. [ Flow Pull - Mostrar reviews del usuario actual ]

        - Flow Pull (LGA_NKS_Flow_Pull v3.49): al hacer click en una fila de la ventana de resultados, el In/Out ahora se toma primero del clip correspondiente en el track `EditRef`, usando la misma logica del ViewerTL Prev/Next Rev. Si no hay track/match EditRef o el helper no esta disponible, conserva el fallback anterior y usa el clip de la fila. [ Flow Pull - Usar EditRef al navegar filas ]

        - Flow Pull (LGA_NKS_Flow_Pull v3.48): la ventana de resultados ahora muestra un titulo arriba a la izquierda con `ProjectName / SeqNumber` usando los mismos colores que el header de Import Shot (proyecto en cyan, separador gris, seq en magenta), y suma abajo un checkbox `Keep this window on top` que arranca prendido por defecto y persiste en `%APPDATA%/LGA/HieroTools/FlowPull.ini`. El `always on top` se togglea con Win32 `SetWindowPos` (HWND_TOPMOST/HWND_NOTOPMOST) en lugar de `setWindowFlags`: este ultimo destruye y recrea la ventana nativa y ESE recreate es el parpadeo (y ademas minimizaba la ventana al destildar). `SetWindowPos` solo cambia el z-order, sin recrear, asi que no parpadea ni se minimiza. La ventana pasa a ser `QDialog`. El recalculo de tamano de la ventana contempla el alto del titulo, la fila del checkbox y los gaps/margenes del layout, ademas del ancho minimo para que titulo y checkbox no queden recortados. [ Flow Pull - Titulo Project/Seq y checkbox Keep on top ]
        - Flow Pull (LGA_NKS_Flow_Pull v3.47): se corrigio el calculo del alto de la ventana de resultados, que siempre dejaba espacio vacio abajo. El alto se sobreestimaba sumando +4 px por fila; ahora se usa `header + suma real de filas + frame de la tabla + margenes del layout`. Se agregaron logs identificados con `[WindowSize]` en `logs/debugPy_FlowPull.log` para diagnosticar el calculo. [ Flow Pull - Corregir alto de la ventana de resultados ]
        
        - Review Pic y el boton Snapshot del ViewerTL Panel ahora tienen en cuenta el pixel aspect ratio (PAR) del formato de la secuencia. El `viewer.image()` ya entrega la imagen con el PAR aplicado (proporciones de display), pero el crop se hacia contra el storage aspect (`width/height`), recortando los lados en timelines con PAR != 1 (ej: PAR 1.8 -> canvas muy chico en X, imagen cortada). Ahora el crop usa el DISPLAY aspect (`width/height * pixelAspect`), conservando toda la imagen sin distorsionarla. [ Review Pic y Snapshot - Respetar pixel aspect ratio del timeline ]
        - Projects Panel corrige el inicio para usuarios que no tienen permitido ver el toggle Studio/Client. Los atributos de los botones se inicializaban en `None`, pero `setup_connections()` intentaba acceder igualmente a `.clicked` porque solo verificaba que los atributos existieran, provocando `AttributeError` durante la carga. Ahora las señales se conectan únicamente cuando ambos botones fueron creados. [ Projects Panel - Corregir inicio sin toggle de contexto ]

v3.77:
        - Shot Info ahora lee y muestra los replies de `version_note_replies` como hilos anidados debajo de cada comentario. Se corrigio el estilo para que la linea vertical izquierda pertenezca solo al contenedor del hilo, sin pequenas lineas adicionales delante del autor ni del texto. [ Shot Info - Mostrar hilos y corregir linea vertical ]

v3.76:
        - Create v000 selecciona `comp` por defecto en contexto client, reemplaza `Create v000 (All tabs)` por un contador dinámico `Create v000 (X shots)`, y muestra `ShotName | TASK` con colores de shot/task en el aviso de V000 ya existente, recalculando su ancho mínimo. Import Shot y Create v000 agregan además la variable `ANCHO_TAB_EXRA = 15`, marcada con `✅✅📛📛`, para sumar margen configurable a ambos lados de todos los tabs. [ Create V000 - Mejorar defaults, contador, avisos y ancho de tabs ]
        - Contexto client: Create v000 ahora restringe tasks a `comp` (sin `roto`/`cleanup`). La UI muestra solo el botón de comp y el filtro de elegibilidad/solape en timeline se calcula únicamente sobre el track `_comp_`. El flujo post-import de Import Shot (single + bulk) hereda esta regla automáticamente al abrir Create v000 con contextos explícitos por shot. [ Contexto Client - Create V000 solo comp ]
        - Import Shot coloca deshabilitadas en el timeline las versiones V000 ya existentes que se importan desde publish, tanto en single como en bulk. [ Import Shot - Deshabilitar V000 existentes al importar ]
        - Create v000 se unifica en una sola UI para single y bulk: ahora siempre abre con tabs por shot (1 shot = 1 tab), reutiliza el mismo shell visual de tabs de Import Shot, soporta detección de múltiples shots desde selección de timeline o targets explícitos, permite ejecutar `Create v000 (All tabs)` en lote, y filtra tasks por shot deshabilitando `comp/roto/cleanup` cuando ya hay clips superpuestos en sus tracks para el rango del shot. Si un shot ya tiene las tres tasks ocupadas, se omite del tabulado; si todos están completos, se informa y no abre tabs vacíos. [ Create V000 - Unificar tabs single/bulk y bloquear tasks con versiones existentes ]
        - Import Shot integra el nuevo flujo de Create v000 contextual: en single, `Import and Create V000` envía contexto explícito del shot importado (`shot_root`, `shot_code`, `timeline_in/out`); en bulk se elimina el botón `Cancel` del footer y se agrega `Import All (N shots) and Create V000`, que arma payload por shot importado y abre Create v000 tabulado sin depender del playhead. [ Import Shot - Agregar Import All + Create V000 y quitar Cancel en bulk ]
        - El sidebar del browser de Import Shot cambia los labels VFX al formato de ruta `T:/VFX-PROJA` / `N:/VFX-PROJB`. Las columnas Type y Date Modified ahora se autoajustan según header y contenido, con mínimos configurables para que sus títulos y valores no queden recortados. [ Import Shot - Ajustar labels y columnas del browser ]
        - El browser custom de Import Shot ahora abre con tamaño configurable (`SHOT_BROWSER_WIDTH/HEIGHT`), usa selección gris/blanca configurable en lugar del highlight amarillo y amplía 50 px el panel izquierdo. El sidebar conserva sus shortcuts estándar y agrega carpetas `VFX-*` detectadas en las raíces `T:/` y `N:/`, identificadas con labels de ruta como `T:/VFX-PROJA`. Todas las variables visuales están marcadas con `✅✅💾⚠️` para edición rápida. [ Import Shot - Mejorar browser y accesos VFX ]
        - El popup del dropdown Track ahora pinta cada opción con la misma paleta usada por Preview y por el valor cerrado del combo. `sin track` aparece rojo, Plate/EditRef/Comp/Roto/Cleanup/DMP usan su color correspondiente y, cuando la fila pertenece a una versión vieja, las opciones usan la variante greyed. `+ Crear track` conserva su estilo verde especial. Aplica en single y bulk mediante el delegate compartido. [ Import Shot - Colorear opciones del dropdown Track ]
        - La columna Track de Import Shot ahora usa los mismos colores que los nombres de track en Preview para Plate, EditRef, Comp, Roto, Cleanup y DMP. Las versiones viejas conservan el tono de su task pero se muestran mezcladas hacia gris oscuro; `— sin track —` permanece rojo y bold, y tracks sin tipo conocido permanecen grises. La función de color es compartida por single, tabs de Bulk y ambos previews. [ Import Shot - Colorear columna Track como Preview ]
        - Los chips V000 nuevos del Preview combinan la paleta base con la identidad de su task: mantienen fondo y texto V000, pero el borde usa específicamente la variable `_PREVIEW_<TASK>_BORDER_COLOR` correspondiente a comp, roto, cleanup, dmp u otro track, sin reutilizar los colores `_CLR_*` de la tabla. En Bulk se transportan por separado el color gris de Hiero/BinItem y el color original de task para Preview, evitando perder la task antes de resolver su borde. Los clips de contexto conservan prioridad absoluta de la paleta greyed aunque sean V000, por lo que shots existentes, anteriores y posteriores permanecen grises en todos los tracks. `_dmp_` pasa a clasificarse explícitamente como tipo `dmp` en el helper de preview. [ Import Shot - Usar borde de task en chips V000 ]
        - Import Shot centraliza las paletas de chips de Preview para single y bulk: plate, editref, comp, roto, cleanup, dmp, other y v000 tienen ahora variables independientes de fondo, borde y texto, manteniendo exactamente los valores visuales anteriores; contexto conserva su tripleta greyed y BurnIn suma una variable propia. También se elimina completamente el botón `Go Back` del tab Preview y su código muerto. [ Import Shot - Centralizar colores de chips Preview ]
        - Import Shot corrige el color de los headers nuevos en Preview: aunque `_PREVIEW_NEW_SHOT_HEADER_COLOR` ya existía, el `color` global de `QHeaderView::section` en el stylesheet pisaba el `ForegroundRole`, por lo que el verde no se veía. El nuevo `_PreviewHeaderView` pinta cada sección directamente: `Shot Nuevo` queda verde en single y todos los shot names nuevos quedan verdes en Bulk; los existentes mantienen gris configurable. Se agregan logs con columna, shot, estado nuevo/existente y color aplicado. [ Import Shot - Aplicar verde real a headers nuevos ]
        - Import Shot corrige consistencia de Preview/Track Assignment en single+bulk: el dropdown de Track ahora usa el orden visual real del timeline (igual que Preview), desambiguando duplicados por nombre como `Track (1)`, `Track (2)` con clave interna única `name||bt_index`; al reasignar un track, el clip previo pasa automáticamente a `— sin track —`; Preview y colocación real respetan esa instancia exacta de track incluso con homónimos (ej. dos EditRef). Además, Bulk Preview incorpora sección `SIN TRACK ASIGNADO`; y los clips de contexto (anterior/posterior/existentes) pasan a estilo greyed-out configurable por variables, junto con color verde configurable para headers de shots nuevos. [ Import Shot - Orden de tracks + claves unicas + preview bulk sin track ]
        - Import Shot individual reemplaza el botón `Preview Timeline` por un tab principal `PREVIEW` ubicado a la derecha de `IMPORT`, siguiendo el patrón del Bulk Import. La página gráfica existente pasa al stack principal, se refresca al entrar al tab, solo se habilita cuando hay ítems marcados y `Go Back` vuelve al tab `IMPORT`. [ Import Shot - Mover Preview Timeline a un tab ]
        - Import Shot (Bulk) elimina el marco extra alrededor de cada tabla: `_BulkShotPanel` reutiliza métodos de `ImportShotDialog` y por eso es un `QDialog` embebido, pero ahora anula el borde global de `_DIALOG_STYLE` para verse igual que el `QWidget` del import individual. Además, el tab bar dibuja separadores verticales `#383838` de altura completa solamente entre dos tabs no seleccionados; los límites junto al tab activo quedan abiertos y conservan su borde más claro, y el último tab inactivo (`PREVIEW` habitualmente) suma también su borde derecho exterior. [ Import Shot - Igualar panel Bulk y separar tabs inactivos ]
        - Import Shot resalta en rojo y bold el valor `— sin track —` de la columna Track, tanto al cargar la tabla como al cambiar asignaciones, resolver conflictos o refrescar las opciones después de crear tracks. [ Import Shot - Resaltar items sin track ]
        - Import Shot (Bulk) deja 2 px libres sobre los tabs para que el borde superior del tab activo no quede pegado a la barra de título. Además, el `QTabBar` ahora usa `QSizePolicy.Expanding` dentro del header, aprovechando todo el ancho disponible antes de activar las flechas de navegación; los tabs individuales conservan su ancho natural con `setExpanding(False)`. El log agrega ancho del header, suma de anchos de tabs, espacio libre y estado/geometría de los botones de scroll. [ Import Shot - Ajustar margen y scroll de tabs Bulk ]
        - Import Shot (Bulk) corrige la compresión vertical del header custom: `_ImportShotTabBar.tabSizeHint()` ahora devuelve una altura mínima de 48 px, de modo que el layout reserve espacio suficiente para los labels y el padding vertical. Los logs `Bulk tabs [...]` incorporan alto real y `sizeHint` del tab bar, header y cada tab para confirmar el comportamiento dentro de Hiero. [ Import Shot - Corregir altura y log de tabs Bulk ]
        - Import Shot (Bulk) corrige un follow-up de tabs: el header vuelve a mostrar correctamente el ancho visible de labels (sin quedar recortados), se evita el `AttributeError` en `_BulkShotPanel` al cambiar tabs (`_refresh_header_layout` ahora se ejecuta solo cuando existe header real), y se ajusta persistencia del browser para guardar la carpeta navegada en el diálogo en lugar de abrir luego dentro del shot seleccionado. [ Import Shot - Fix follow-up tabs bulk y carpeta inicial del browser ]
        - Import Shot (Bulk) corrige la UI de tabs para que use el mismo header custom del import individual (`_ImportShotTabBar` + `_HeaderSeparator` + `QStackedWidget`): los shot names vuelven a leerse completos con scroll natural y el tab activo recupera el hueco del separador (sin línea inferior). Se agregan logs `Bulk tabs [...]` en `debugPy_ImportShots.log` para diagnosticar anchos/rects de tabs. [ Import Shot - Fix tabs del Bulk con header custom y logs ]
        - Import Shot incorpora Bulk Import: el browser Qt permite seleccionar varias carpetas con Ctrl/Shift; los shots se presentan en tabs editables de ancho natural, con scroll cuando no entran y sin recortar shot names ni `PREVIEW`; el preview grafico combinado recupera las barras por track, chips coloreados y proporcionales, offsets de EditRef, tooltips y BurnIn del preview individual, mostrando en gris desaturado los shots existentes del contexto; el footer resume los omitidos como `N Shots omitidos` y deja los nombres en el tooltip; Rename/Transcode/Open Queue quedan temporalmente ocultos mediante el flag global `RENAME_TRANSCODE_TABS = False`; y el batch completo se inserta en orden alfabetico dentro de un unico bloque de undo. [ Import Shot - Ajustar tabs y resumen del Bulk Import ]
        - `get_pipesync_db_path` (`LGA_NKS_PipeSyncPaths`) ahora resuelve la DB/cache de PipeSync SIEMPRE a la instalación estándar (`C:/Portable/LGA/PipeSync/cache` para studio, `.../cacheClient` para client) e ignora a propósito el `CachePath` guardado en `config.secure`. Antes, si el `config.secure` traía un `CachePath` apuntando a un build de desarrollo (`C:/Portable/LGA_PipeSync_2/...`) y esa ruta existía, pull/push/playlist/shot-info/assignee leían/escribían en el build en vez del instalado. El caso se detectó en modo client: el `config.secure` del client apuntaba a `LGA_PipeSync_2/cacheClient` (que existía) y por eso el Pull abría una DB del build vacía; studio "funcionaba" solo de casualidad porque su `CachePath` del build no existía y caía al fallback instalado. Ahora es consistente y honesto para todos los consumidores (todos pasan por `get_pipesync_db_path`). [ PipeSync Paths - Usar siempre la cache instalada, nunca el build ]
        - El Pull (`LGA_NKS_Flow_Pull`) ahora distingue claramente "DB vacía / sin sincronizar" de "no hay cambios reales". Antes, en modo client (donde la `pipesync.db` existe pero está vacía porque Flow nunca se sincronizó), el Pull abría la DB vacía, no encontraba ningún shot y mostraba el engañoso "No changes were detected in the selected shots". Ahora se chequea `count_projects()` apenas se abre la DB: si hay 0 proyectos, muestra un warning indicando que la DB está vacía y hay que configurar/sincronizar Flow en ese contexto. Además se agregaron contadores `shots_found_in_db` / `shots_not_found`: si la DB tiene datos pero ninguno de los shots del timeline aparece, avisa que puede no estar sincronizada o que los nombres no coinciden, en vez del mensaje genérico de "sin cambios". [ Flow Pull - Distinguir DB vacía de "sin cambios" ]
        - Se reemplazó el botón de switch Studio/Client por un toggle pill estilo segmentado (dos segmentos `👤 client` / `🎬 studio`) ubicado alineado a la derecha de la línea de "X proyectos encontrados, Y abiertos", en vez del botón gris en la columna derecha de botones. El segmento activo se pinta en violeta (`#443a91`) y el inactivo en gris transparente con hover. Clickear un segmento cambia directo al contexto correspondiente (sin popup de confirmación; la recarga del panel es el feedback). Nuevos métodos `set_context_mode()` y `_refresh_context_toggle()` en el panel, y `UIManager._build_context_toggle()`. [ Projects Panel - Toggle pill Client/Studio a la derecha ]
        - Se implementó el botón de switch Studio/Client en el Projects Panel: nuevo botón debajo del botón de refresh que permite cambiar entre contexto `studio` (PipeSync normal) y `client` (PipeSyncClient), visible únicamente para el usuario `lega@wanka.tv`. El cambio se persiste en `LGA_HieroTools_context.ini` y en variables de entorno (`LGA_HIEROTOOLS_CONTEXT_INI` + `PIPESYNC_CONTEXT`) para que pull/push usen credenciales del contexto correcto. Se corrigió un bug donde el UIManager no estaba creando el botón porque el código estaba en un método `setup_ui()` del panel que nunca se ejecutaba; se migró la lógica al `UIManager.setup_ui()` y `setup_connections()` con inicialización de dependencias correcta. Se agregó debug logging detallado en `_get_normal_pipesync_login()` y en el UIManager para diagnosticar si el botón se muestra o no. [ Projects Panel - Switch Studio/Client visible ]

v3.75:
        - Inicialización de proyecto
